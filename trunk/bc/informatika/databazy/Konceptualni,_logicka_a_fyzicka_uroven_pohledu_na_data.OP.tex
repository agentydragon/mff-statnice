\subsection{Konceptualní, logická a fyzická úroveò pohledu na data}

TODO: sjednotit terminologii, snad to popisuje to co tu má být, ale zdroje jsou pochybné (Wikipedie tady neodvádí zrovna ideální práci a ÈVUT Wiki se moc nerozepisuje).

\begin{e}{Definice}{0}{Datové modelování}
\emph{Datové modelování} je proces vytvoøení konkrétního datového modelu (schématu) databáze pomocí aplikace nìjakého abstraktního databázového modelu. Datové modelování zahrnuje kromì definice struktury a organizace dat ještì další implictiní nebo explicitní omezení na data do struktury ukládaná. 
\end{e}

\begin{obecne}{Vrstvy modelování}
Druhy datových modelù mohou být tøí typù, podle tøí rùzných pohledù na databáze (tøi \uv{vrstvy}, které se navzájem doplòují):
\begin{pitemize}
    \item konceptuální schéma (datový model) -- nejabstraktnìjší, popisuje význam organizace databáze -- tøídy entit a jejich vztahy.
    \item logické schéma -- popisuje význam konceptuálního schématu z hlediska databázové implementace -- popisy tabulek, programových tøíd nebo XML tagù (podle zvoleného databázového modelu)
    \item fyzické schéma -- nejkonkrétnìjší, popisuje fyzické uložení dat a stroje na kterých systém pobìží.
\end{pitemize}
Na tomto rozdìlení je dùležitá nezávislost jednotlivých vrstev -- takže se implementace jedné z nich mùže zmìnit, aniž by bylo nutné výraznì upravovat ostatní (samozøejmì musí zùstat konzistetní vzhledem k ostatním vrstvám). Bìhem implementace nìjaké databázové aplikace se zaèíná vytvoøením konceptuálního schématu, pokraèuje jeho upøesnìní logickým schématem a naknec jeho fyzickou implementací podle fyzického schématu (modelu).
\end{obecne}

\begin{e}{Poznámka}{0}{0}
V tomto pohledu (který je podle standardu ANSI z r. 1975) jsou databázové modely, popsané v pøedchozí sekci, pøíklady abstraktních logických datových modelù. Nìkde je však tato úroveò oznaèována jako \uv{fyzická} a \uv{jiná logická} se vtìsní ještì mezi ni a konceptuální.
\end{e}


\begin{obecne}{Konceptuální schéma}
\emph{Konceptuální schéma} (datový model) popisuje podstatné objekty (\emph{tøídy entit}, \uv{koncepty}), jejich charakteristiky (\emph{atributy}) a vztahy mezi nimi (asociace mezi dvojicemi tøíd entit). Nepopisuje pøímo implementaci v databázi, jen význam nìjakého celku, který bude databází pøedstavován. Jde o modelování \uv{datové reality}, z pohledu uživatele (analytika, konstruktéra databáze).

\medskip
\begin{e}{Pøíklady}{0}{0}
Pár pøíkladù vztahù mezi tøídami entit (z Wikipedie):
\begin{pitemize}
    \item Each PERSON may be the vendor in one or more ORDERS.
    \item Each ORDER must be from one and only one PERSON.
    \item PERSON is a sub-type of PARTY. (Meaning that every instance of PERSON is also an instance of PARTY.)
\end{pitemize}
\end{e}
De-facto standardem pro konceptuální datové modelování jsou \emph{ER-diagramy} (entity-relationship diagramy). Hodí se hlavnì pro \uv{plochá} formátovaná data (takže tøeba pro objektové nebo relaèní databáze, ale ne pro XML apod.). Používají dva typy \uv{objektù} -- \emph{entity} (tøídy entit) a \emph{vztahy}. Jde o obdobu UML z objektového programování. Pøíklad ER-diagramu se vztahem dvou entit je na následujícím obrázku (popisuje i další vlastnosti -- atributy entit a kardinality vztahù):
\begin{center}
\includegraphics[width=14cm]{informatika/databazy/obrazky/er-schema.png}

(Obrázek je upravený, rozšíøený a popsaný pøíklad ze slidù Dr. T. Skopala k Databázovým systémùm)
\end{center}
\end{obecne}

\begin{obecne}{Logické schéma}
\emph{Logické schéma} je datový model organizace nìjakého specifického celku pomocí jednoho z databázových modelù -- podle databázových modelù popsaných v pøedchozí sekci, tj. napø. pomocí relaèních tabulek, objektových tøíd nebo XML. Svojí úrovní abstrakce se nachází mezi konceptuálním a fyzickým schématem.
\end{obecne}

\begin{obecne}{Fyzické schéma}
\emph{Fyzické datové modely} jsou modely, ktere používají databazové stroje smìrem k nižším vrstvám (operaèního) systému. V zásadì jde o rùzné zpùsoby fyzického uložení dat (tedy schémata organizace souborù) -- sekvenèní soubory, B-stromy apod.
\end{obecne}

