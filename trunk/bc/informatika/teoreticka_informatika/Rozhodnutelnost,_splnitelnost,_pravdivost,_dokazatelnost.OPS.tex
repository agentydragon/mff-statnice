\def\c#1{\mathcal{#1}}
\def\Nat{\mathbb{N}}


\subsection{Rozhodnutelnost, splnitelnost, pravdivost a dokazatelnost}

\begin{obecne}{}
Z tìchto témat se rozhodnutelnosti budeme vìnovat a jako poslední, protoe k vyslovení nìkterıch vìt budeme potøebovat pojmy definované v èástech o splnitelnosti, pravdivosti a dokazatelnosti.
\end{obecne}

\begin{definiceN}{Formální systém vırokové logiky}
Pracujeme s redukovanım jazykem (jen s log. spojkami $\neg,\rightarrow$). Formální systém vırokobé logiky obsahuje:
\begin{penumerate}
    \item \emph{jazyk $L_P$} vırokové logiky nad mnoinou prvotních formulí $P$,
    \item \emph{schémata axiomù vırokové logiky}, ze kterıch pro libovolné formule $A, B, C$ jazyka $L_P$ vznikají axiomy tvaru
    \begin{penumerate}
        \item $A \to (B \to A)$ \hfill (A1 - \uv{implikace sebe sama}),
        \item $\left(A \to \left(B \to C\right)\right) \to \left[\left(A \to B\right) \to \left(A \to C\right) \right]$ \hfill (A2 - \uv{roznásobení}),
        \item $\left(\neg B \to \neg A\right) \to \left(A \to B\right)$ \hfill (A3 - \uv{obrácená negace implikace}),
    \end{penumerate}
    \item odvozovací pravidlo ($modus\ ponens$) -- z formulí $A$ a $A \to B$ odvoï formuli $B$.
\end{penumerate}
\end{definiceN}


\begin{definiceN}{Formální systém predikátové logiky}
Pracujeme s redukovanım jazykem (jen s log. spojkami $\neg,\rightarrow$ a jen s kvantifikátorem $\forall$). \emph{Schémata axiomù predikátové logiky} vzniknou z tìch ve vırokové logice prostım dosazením libovolnıch formulí predikátové logiky za vırokové promìnné. \emph{Modus ponens} platí i v pred. logice. PL obsahuje navíc další dva axiomy a odvozovací pravidlo:
\begin{pitemize}
    \item \emph{schéma specifikace}: $(\forall x)A\rightarrow A_x[t]$
    \item \emph{schéma pøeskoku}: $(\forall x)(A\rightarrow B)\rightarrow (A\rightarrow(\forall x)B)$, pokud 
	promìnná $x$ nemá volnı vıskyt v $A$.
    \item \emph{pravidlo generalizace}: $\frac{A}{(\forall x)A}$
\end{pitemize}
Toto je formální systém pred. logiky \emph{bez rovnosti}. S rovností pøibıvá symbol $=$ a další tøi axiomy.
\end{definiceN}

\begin{definiceN}{splnitelnost\vl}
Formule $A$ ve vırokové logice je \emph{splnitelná}, jestlie existuje ohodnocení $e$ takové, e $A$ je pravdivá pøi $e$. Mnoina formulí $T$ je splnitelná, pokud existuje ohodnocení $e$ takové, e kadá formule $A\in T$ je pravdivá pøi $e$. Potom $e$ nazıváme \emph{modelem teorie} $T$ (znaèíme $e\models T$).
\end{definiceN}

\begin{definiceN}{dùkaz\vl}
Dùkaz $A$ je koneèná posloupnost formulí $A_1,\dots A_n$, jestlie $A_n = A$ a pro kadé $i=1,..n$ je $A_i$ buï axiom, nebo je odvozená z pøedchozích pravidlem modus ponens. Existuje-li dùkaz formule $A$, pak je tato \emph{dokazatelná} ve vırokové logice (je vìtou vırokové logiky, znaèíme $\vdash A$).
\end{definiceN}

\begin{definiceN}{dùkaz z pøedpokladù}
Dùkaz formule $A$ z pøedpokladù je posloupnost formulí $A_1,\dots A_n$ taková, e $A_n = A$ a $\forall i\in\{1,..n\}$ je $A_i$ axiom, nebo prvek mnoiny pøedpokladù $T$, nebo je odvozena z pøechozích pravidlem modus ponens. Existuje-li dùkaz $A$ z $T$, pak $A$ \emph{je dokazatelná} z $T$, znaèíme $T\vdash A$.
\end{definiceN}

\begin{vetaN}{o dedukci\vl}
Pro $T$ mnoinu formulí a formule $A,B$ platí: $$T\vdash A\rightarrow B \mbox{ právì kdy } T,A\vdash B$$
\end{vetaN}
\begin{ideadukazu}
\begin{pitemize}
    \item[$\to$] Máme dùkaz formule $A \to B$, k nìmu mùeme z pøedpokladu pøipojit $A$ a pomocí MP odvodit $B$.
    \item[$\leftarrow$] $A_1, \ldots, A_n = B$ je dùkaz formule $B$ z pøedpokladù $T, A$. Indukcí dokáeme, e $T \vdash A \to A_i$, tedy pro $i = n$ jsme hotovi.
\end{pitemize}
\end{ideadukazu}

\begin{dusledek}
Pro libovolnou mnoinu formulí $T$ a formule $A, B, C$ platí:
$$T \vdash A \to \left(B \to C\right) \text{ právì kdy } T, A, B \vdash C \text{ právì kdy } T \vdash B \to \left(A \to C\right),$$
$$T \vdash \left(A \to \left(B \to C\right)\right) \to \left(B \to \left(A \to C\right)\right),$$
$$\vdash \left(A \to B\right) \to \left[\left(B \to C\right) \to \left(A \to C\right)\right].$$
Poslednímu vztahu øíkáme \emph{vìta o skládání implikací}, vıše je ukázáno, e v implikaci nezáleí na poøadí pøedpokladù.
\end{dusledek}

\begin{vetaN}{o neutrální formuli\vl}
Nech $T$ je mnoina vırokovıch formulí, nech $A, B$ jsou formule. Jestlie $T, A \vdash B$ a $T, \neg A \vdash B$, pak $T \vdash B$.
\end{vetaN}

\begin{definiceN}{Tarského definice pravdy\pl}
Pro danı (redukovanı, tj. jen se \uv{základními} log. spojkami) jazyk predikátové logiky $L$, $\c{M}$ jeho interpretaci, ohodnocení $e$ a formuli $A$ tohoto jazyka platí:
\begin{penumerate}
    \item $A$ je \emph{pravdivá v $\mathcal{M}$ pøi ohodnocení} $e$ nebo \emph{platná v $\c{M}$ pøi ohodnocení} $e$ (znaèíme $\c{M}\models A[e]$), kdy:
    \begin{pitemize}
	\item $A$ je atomická tvaru $p(t_1,\dots,t_n)$, kde $p$ není rovnost a $(t_1[e],\dots,t_n[e])\in p_M$.
	\item $A$ je atomická tvaru $t_1 = t_2$ a $t_1[e]=t_2[e]$
	\item $A$ je tvaru $\neg B$ a $\c{M}\not\models B[e]$
	\item $A$ je tvaru $B\rightarrow C$ a $\c{M}\not\models B[e]$ nebo $\mathcal{M}\models C[e]$
	\item $A$ je tvaru $(\forall x)B$ a $\c{M}\models B[e(x/m)]$ pro kadé $m\in M$
	\item $A$ je tvaru $(\exists x)B$ a $\c{M}\models B[e(x/m)]$ pro nìjaké $m\in M$
    \end{pitemize}
    \item $A$ je \emph{pravdivá v interpretaci} $\mathcal{M}$ nebo \emph{platná v interpretaci} $\c{M}$ ($\c{M}\models A$), jestlie je $A$ pravdivá v $M$ pøi kadém ohodnocení promìnnıch (pro uzavøené formule staèí jedno ohodnocení, splnìní je vdy stejné)
   \end{penumerate}
\end{definiceN}

\begin{definiceN}{logicky pravdivá/platná formule\pl}
Formule $A$ je \emph{validní (logicky pravdivá/pravdivá)} ($\models A$), kdy je platná pøi kadé interpretaci daného jazyka.
\end{definiceN}

\begin{definiceN}{spornost, bezespornost}
Mnoina formulí $T$ je \emph{sporná}, pokud je z pøedpokladù $T$ dokazatelná libovolná formule, jinak je $T$ \emph{bezesporná}. $T$ je \emph{maximální bezesporná} mnoina, pokud je $T$ bezesporná a navíc jediná její bezesporná nadmnoina je $T$ samo. Mnoina všech formulí dokazatelnıch z $T$ se znaèí $\mathit{Con}(T)$.
\end{definiceN}

\begin{vetaN}{o bezespornosti a splnitelnosti}
Mnoina formulí vırokové logiky je bezesporná, právì kdy je splnitelná.
\end{vetaN}

\begin{poznamkaN}{vlastnosti formulí predikátové logiky}
\begin{penumerate}
    \item Je-li $A'$ instance formule $A$, pak jestlie platí $\vdash A$, platí i $\vdash A'$.
	(\emph{Vìta o instancích})
    \item Je-li $A'$ uzávìr formule $A$, pak $\vdash A$ platí právì kdy $\vdash A'$. 
	(\emph{Vìta o uzávìru})
\end{penumerate}
\end{poznamkaN}

\begin{vetaN}{o dedukci\pl}
Nech $T$ je mnoina formulí pred. logiky, $A$ je uzavøená formule a $B$ lib. formule, potom $T\vdash A\rightarrow B$ právì kdy $T,A\vdash B$.
\end{vetaN}

\begin{definiceN}{teorie, model -- obecnì}
Pro nìjakı jazyk $L$ prvního øádu je mnoina $T$ formulí tohoto jazyka \emph{teorie prvního øádu}. Formule z $T$ jsou \emph{speciální axiomy} teorie $T$. Pro interpretaci $\c{M}$ jazyka $L$ je $\c{M}$ \emph{model teorie} $T$ (znaèíme $\c{M}\models T$), pokud jsou všechny speciální axiomy $T$ pravdivé v $\c{M}$. Formule $A$ je \emph{sémantickım dùsledkem} $T$ (znaèíme $T\models A$), jestlie je pravdivá v kadém modelu teorie $T$.
\end{definiceN}

\begin{definiceN}{rekurzivní funkce a mnoina}
\emph{Rekurzivní funkce} jsou všechny funkce popsatelné jako $f:\Nat^k\to\Nat$, kde $k\geq 1$, tedy všechny \uv{algoritmicky vyèíslitelné} funkce. Mnoina pøirozenıch èísel je \emph{rekurzivní mnoina (rozhodnutelná mnoina)}, pokud je rekurzivní její charakteristická funkce (funkce urèující, které prvky do mnoiny patøí).
\end{definiceN}

\begin{definiceN}{spoèetnı jazyk, kód formule}
\emph{Spoèetnı jazyk} je jazyk, kterı má nejvıš spoèetnì mnoho speciálních symbolù. Pro spoèetnı jazyk, kde lze efektivnì (rekurzivní funkcí) oèíslovat jeho speciální symboly, lze kadé jeho formuli $A$ pøiøadit její \emph{kód formule} - pøir. èíslo $\#A$.
\end{definiceN}

\begin{vetaN}{Churchova o nerozhodnutelnosti predikátové logiky}
Pokud spoèetnı jazyk $L$ prvního øádu obsahuje alespoò jednu konstantu, alespoò jeden funkèní symbol arity $k>0$ a pro kadé pøirozené èíslo spoèetnì mnoho predikátovıch symbolù, potom mnoina $\{\#A|A \text{ je uzavøená formule a }L\models A\}$ není rozhodnutelná.
\end{vetaN}

\begin{vetaN}{o nerozhodnosti predikátové logiky}
Nech $L$ je jazyk prvního øádu bez rovnosti a obsahuje alespoò 2 binární predikáty. Potom je predikátová logika (jako teorie) s jazykem $L$ nerozhodnutelná.
\end{vetaN}

\begin{definiceN}{Tøi popisy aritmetiky}
Je dán jazyk $L=\{0,S,+,\cdot\,\leq\}$.
\begin{pitemize}
    \item \emph{Robinsonova aritmetika} - "$Q$" s jazykem L má 8 násl. axiomù:
    \begin{penumerate}
	\item $S(x)\neq 0$
	\item $S(x)=S(y)\rightarrow x=y$
	\item $x\neq 0\rightarrow (\exists y)(x=S(y))$
	\item $x+0=x$
	\item $x+S(y)=S(x+y)$
	\item $x\cdot 0=0$
	\item $x\cdot S(y)=(x\cdot y)+x$
	\item $x\leq y\leftrightarrow (\exists z)(z+x=y)$
    \end{penumerate}

    \textit{Poznámka: Nìkdy, pokud není potøeba definovat uspoøádání, se poslední axiom spolu se symbolem \uv{$\leq$} vypouští.}

    \item \emph{Peanova aritmetika} - "$P$" má všechny axiomy Robinsonovy kromì tøetího, navíc má 
	\emph{Schéma(axiomù) indukce} - pro formuli $A$ a promìnnou $x$ platí: $A_x[0]\rightarrow 
	\{(\forall x)(A\rightarrow A_x[S(x)])\rightarrow(\forall x)A\}$.
    \item \emph{Úplná aritmetika} má za axiomy všechny uzavøené formule pravdivé v $\Nat$, je-li $\Nat$
	standardní model aritmetiky - \uv{pravdivá aritmetika}. \emph{Teorie modelu $\Nat$} je mnoina
	$Th(\Nat)=\{A|A\text{ je uzavøená formule a } N\models A\}$.
\end{pitemize}
Platí: $Q\subseteq P\subseteq Th(\Nat)$. $Q$ má koneènì mnoho axiomù, je tedy rekurzivnì axiomatizovatelná. $P$ má spoèetnì mnoho axiomù, kódy axiomù schématu indukce tvoøí rekurzivní mnoinu. $Th(\Nat)$ není rekurzivnì axiomatizovatelná.
\end{definiceN}


\begin{definiceN}{Mnoina kódù vìt teorie}
Pro $T$ teorii s jazykem aritmetiky definujeme \emph{mnoinu kódù vìt teorie} $T$ jako $Thm(T)=\{\#A|A \text{ je uzavøená formule a } T\vdash A\}$.
\end{definiceN}

\begin{definiceN}{Rozhodnutelná teorie}
Teorie $T$ s jazykem aritmetiky je \emph{rozhodnutelná}, pokud je mnoina $Thm(T)$ rekurzivní. V opaèném pøípadì je $T$ \emph{nerozhodnutelná}.
\end{definiceN}

\begin{vetaN}{Churchova o nerozhodnutelnosti aritmetiky}
Kadé bezesporné rozšíøení Robinsonovy aritmetiky $Q$ je nerozhodnutelná teorie.
\end{vetaN}

\begin{vetaN}{Gödel-Rosserova o neúplnosti aritmetiky}
ádné bezesporné a rekurzivnì axiomatizovatelné rozšíøení Robinsonovy aritmetiky $Q$ není úplná teorie.
\end{vetaN}