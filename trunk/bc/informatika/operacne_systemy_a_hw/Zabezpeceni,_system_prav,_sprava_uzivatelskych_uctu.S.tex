\subsection{Zabezpeèení, systém práv, správa uživatelských úètù}

TODO: tohle je jenom copy \& paste z Wiki

\subsubsection*{Zabezpeèení}

\begin{pitemize}
  \item procesy bìží v uživatelském režimu s omezenými možnostmi, pøi systémových voláních se proces pøepne do režimu jádra
  \item preemptivní plánování, priority
  \item firewall - iptables (Linux), pf (OpenBSD), ipfw (FreeBSD), FW-1 (Solaris), ipfilter aj.
  \item vypnutí nepotøebných služeb (daemonù)
  \item zálohování
  \item sledování logù
  \item nmap
  \item Kerberos
  \item chroot
  \item sifrovani disku
  \item sifrovani komunikace
  \item pouceni uzivatelu
  \item silna hesla 
\end{pitemize}

\subsubsection*{Systém práv}

\begin{pitemize}
  \item každý soubor a proces mají vlastníka a skupinu
  \item práva pro vlastníka, skupinu a ostatní - ètení, zápis, spouštìní
  \item setuid, setgid - propùjèení práv vlastníka/skupiny pøi spuštìní programu
  \item setgid pro adresáøe - nové soubory budou mít stejnou skupinu jako adresáø
  \item sticky bit pro adresáøe - práva k souborùm mají jen vlastníci souborù a nikoliv vlastníci adresáøe
  \item uživatel root
  \item reálné a efektivní UID/GID u bìžících procesù
  \item chmod, chown, chgrp, umask 
\end{pitemize}

\subsubsection*{Správa uživatelských úètù}

\begin{pitemize}
  \item /etc/passwd - seznam uživatelù - login, UID, GID, plné jméno, domovský adresáø, shell
  \item /etc/group - skupiny - název, GID, seznam èlenù
  \item /etc/shadow - zašifrovaná hesla (hash) - mùže èíst pouze root
  \item useradd, userdel, usermod, groupadd, groupdel, groupmod, passwd
\end{pitemize}
