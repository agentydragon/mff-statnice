\subsection{Konkrétní souborové systémy}

\subsubsection*{ext2 (second extended filesystem)}

\begin{pitemize}
  \item Prostor je rozdìlen na bloky a ty jsou uspoøádány do skupin blokù. To je kvùli
  snížení vnitøní fragmentace a minimalizaci pohyb hlav na disku.
  \item Každá skupina blokù obsahuje: superblock, mapu skupiny blokù, mapu inodù
  a bloky dat.
  \item \textbf{superblock}~-- obsahuje základní (nezbytné) informace o svazku. Jeho kopie
  je tedy v každé skupinì blokù 
  \item \textbf{i-node}~-- obsahuje informace o souboru
  \begin{pitemize}
    \item poèet linkù na soubor
    \item vlastník, skupina
    \item pøístupová práva
    \item typ souboru
    \item velikost souboru
    \item èasy~-- modifikace, pøístup\dots
    \item odkazy na datové bloky
    \item neobsahuje název souboru, ten je uložen v adresáøi kam soubor patøí
  \end{pitemize}
  \item velkost FS daná pøi formátování
\end{pitemize}

\subsubsection*{ext3}
\begin{pitemize}
  \item nadstavba na ext2 a tedy možnost pøechodu mezi tìmito dvìma FS bez
  nutnosti pøenášení dat.
  \item hlavní novinka je, že pøibyl žurnál. Jsou možné tøi úrovnì žurnálování:
  \begin{pitemize}
    \item \textbf{Journal}~-- (nejpomalejší, ale nejspolehlivìjší) metadata i
    obsah souborù jsou zapisovány do žurnálu, ještì pøed komitnutím do FS.
    \item \textbf{Ordered}~-- (støednì rychlý, støednì riskantní) 
    \item \textbf{Writeback}~-- (nejrychlejší, ale riskantní - v nìkterých
    ohledech jako ext2) žurnálována jsou pouze metadata, ne již obsah souborù.
  \end{pitemize}
  \item pøi havárii systému okamžitá oprava podle žurnálu, FS se nemusí kontrolovat
  \item FS roste za bìhu jak je potøeba
\end{pitemize}

\subsubsection*{ReiserFS}
\begin{pitemize}
  \item žunálování metadat
  \item rùst velikosti FS za bìhu, dle potøeby
  \item \uv{tail packing} pro snížení míry fragmentace
  \item metadata, adresáøové položky, inody a konce (tail) souborù jsou uloženy
  v jednom \emph{B+ stromì} podle univerzálního ID
  \item mnohdy rychlejší než ext2 (ext3), kde jsou adresáøe jako seznamy souborù
  a tak projití adresáøe je lineární na rozdíl od logaritmického u stromu v ReiserFS.
\end{pitemize}

\subsubsection*{FAT32 (File Allocatio Table)}
\begin{pitemize}
  \item jednoduchá implementace $\Rightarrow$ témìø na každém OS
  \item žádná ochrana proti fragmentaci
  \item zjistit kolik je volného místa znamená projít lineárnì celý disk
  \item rozdìlení disku:
  \begin{pitemize}
    \item Boot sector
    \item FAT~-- dvì kopie FAT (mapy datové oblasti)
    \item Datová oblast~-- soubory a adresáøe až do konce disku
  \end{pitemize}
\end{pitemize}
