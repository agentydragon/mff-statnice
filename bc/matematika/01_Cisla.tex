\section{Èísla}

\begin{pozadavky}
\begin{pitemize}
\item Vlastnosti pøirozenıch, celıch, racionálních, reálnıch a komplexních èísel
\item Posloupnosti a limity
\item Cauchyovské posloupnosti
\end{pitemize}
\end{pozadavky}

\subsection{Reálná èísla}
$\mathbb{R}$ definujeme axiomaticky.

\begin{definice}
Mnoinou všech reálnıch èísel (znaèíme ji $\mathbb{R}$) budeme rozumìt mnoinu, na ní je definováno sèítání (znaèíme $x+y$), násobení (znaèíme $x\cdot y$) a uspoøádání (znaèíme $x \le y$), která spòují tyto axiomy:

\begin{penumerate}
	\item (Algebraické operace)
	\begin{penumerate}
		\item $\forall x,y,z \in \mathbb{R} : x+(y+z) = (x+y)+z \hfill\textit{(asociativní zákon pro sèítání)}$
		\item $\forall x,y \in \mathbb{R} : x+y = y+x \hfill\textit{(komutativní zákon pro sèítání)}$
		\item v $\mathbb{R}$ existuje nulovı prvek (znaèíme ho 0) tak, e $\forall x \in \mathbb{R}: x+0 = x$
		\item pro kadı $x \in \mathbb{R}$ existuje opaènı prvek (znaèíme ho $-x$) tak, e $x+(-x)=0$
		\item $\forall x,y,z \in \mathbb{R}: x\cdot (y\cdot z)=(x\cdot y)\cdot z \hfill\textit{(asociativní zákon pro násobení)}$
		\item $\forall x,y \in \mathbb{R} : x\cdot y = y\cdot x \hfill\textit{(komutativní zákon pro násobení)}$
		\item v $\mathbb{R}$ existuje jednotkovı prvek (znaèíme ho 1) tak, e $\forall x \in \mathbb{R}: x\cdot 1=x$
		\item pro kadı $x \in \mathbb{R}, x \neq 0$ existuje inverzní prvek (znaèíme ho $x^{-1}$) tak, e $x\cdot x^{-1}=1$
		\item $\forall x,y,z \in \mathbb{R}: (x+y)\cdot z = x\cdot z + y\cdot z \hfill\textit{(distributivní zákon)}$
	\end{penumerate}
	\item (Uspoøádání)
	\begin{penumerate}
		\item $\forall x,y,z \in \mathbb{R}: ((x \le y) \wedge (y \le z)) \Rightarrow (x \le z) \hfill\textit{(tranzitivita)}$
		\item $\forall x,y \in \mathbb{R}: (((x \le y) \wedge (y \le x)) \Rightarrow (x=y) \hfill\textit{(slabá antisymetrie)}$
		\item $\forall x,y \in \mathbb{R}: (x \le y) \vee (y \le x)\hfill\textit{(linearita)}$
		\item $\forall x,y,z \in \mathbb{R}: (x \le y) \Rightarrow (x+z \le y+z)$
		\item $\forall x,y,z \in \mathbb{R}: (x \le y) \wedge (0 \le z) \Rightarrow (x\cdot z \le y\cdot z)$
	\end{penumerate}
	\item (Netrivialita)
	\begin{penumerate}
		\item $0 \neq 1$
	\end{penumerate}
	\item (Úplnost)
		\\\begin{definiceN}{Axiom suprema}
		Nech $M \subset \mathbb{R}$ je neprázdná a shora omezená (tj. $\exists a \forall x \in M: x \le a$). Pak existuje èíslo $s \in \mathbb{R}$, které má vlastnosti:
		\begin{penumerate}
			\item $\forall x \in M: x \le s\hfill\textit{(s je horní omezení)}$
			\item $\forall s' \in \mathbb{R}, s' < s\;\; \exists x \in M: x > s'\hfill\textit{(všechny menší nejsou horní omeznení)}$
		\end{penumerate}
		
		\end{definiceN}
\end{penumerate}

\end{definice}
\begin{poznamka}
Axiom supréma je dùleitı, odlišuje reálná èísla od racionálních.


		Èíslo $s$ z~axiomu suprema je jednoznaènì urèeno, znaèí se $\sup M$ a øíká se mu \textbf{supremum mnoiny $M$}. Supremum mnoiny je její nejmenší horní závora (\textbf{horní závora} nebo \textbf{horní mez} je kadı takovı prvek, pro kterı platí bod (a) definice suprema). Nejvìtší dolní závoru mnoiny $M$ nazıváme \textbf{infimem mnoiny $M$} a znaèíme $\inf M$; z~axiomu suprema plyne, e kadá zdola omezená mnoina má infimum.
\end{poznamka}

\begin{poznamka}
Relace \uv{$<$} na reálnıch èíslech (a stejnì tak na pøirozenıch a racionálních èíslech) se definuje takto: $a<b$, právì kdy $a\leq b$ a zároveò $a\neq b$.
\end{poznamka}

\begin{definice}
     $\mathbb{R}$ lze sestrojit i tak, e vezmeme $\mathbb{Q}$ a $\mathbb{R}$ definujeme z~nich -- tento pøístup jsem formálnì popsal zde, ale nejsem si jist, jestli bude nutnı u zkoušek -- nejspíš ne a je tu spíš pro úplnost. Další, ekvivalentní pøístup jsou tzv. Dedekindovy øezy, skrze které byly dokonce reálná èísla pùvodnì vytvoøena\footnote{více info napø. tady -- \url{http://en.wikipedia.org/wiki/Dedekind_cut} -- opìt, nepøedpokládám, e by bylo nutné znát.}.
     
Nech $R$ je mnoina všech Cauchyovskıch posloupností ve $\mathbb{Q}$. Operace na této mnoinì definujeme následovnì:
\begin{itemize}
	\item $\left\{x_n\right\} + \left\{y_n\right\} = \left\{x_n + y_n\right\}$
	\item $\left\{x_n\right\} \cdot \left\{y_n\right\} = \left\{x_n \cdot y_n\right\}$
	\item $\left\{x_n\right\} \ge \left\{y_n\right\}$, právì kdy $\exists n_0\;\; \forall n>n_0: x_n\ge y_n$
\end{itemize}
Na $R$ definujme relaci ekvivalence jako $\left\{x_n\right\} \sim \left\{y_n\right\}  \Leftrightarrow \lim_{k\rightarrow \infty}\left|x_k-y_k\right|=0$ (je nutné ještì dokázat, e jde o ekvivalenci). Potom reálná èísla $\mathbb{R} = R / \sim$. 

\medskip\begin{poznamka}
Na takto definovanıch reálnıch èíslech lze potom racionální èísla vzít jako tøídy ekvivalence konstantních posloupností ($x_0 = x_n = q \in \mathbb{Q}$), celá èísla potom jako tøídy ekvivalence posloupností, kde $x_0 = x_n = n \in \mathbb{N}$.
\end{poznamka}

\medskip\begin{poznamka}
O co pøesnì jde lze dobøe ilustrovat na desetinnıch rozvojích daného reálného èísla: napø. pro èíslo $\pi$ mùe jít napø. o posloupnost $3$, $3,1$, $3,14$, $3,141$, $3,1415$\ldots, ale také napøíklad $3$, $3,14$, $3,1415$\ldots. Jde o rùzné posloupnosti, obì ale reprezentují stejné reálné èíslo $\pi$.

Stejnì je napøíklad èíslo $0,\overline{9}$ rovno èíslu $1$ -- pokud vezmeme $\left\{x_n\right\}$ posloupnost $0,9$, $0,99$\ldots a $\left\{y_n\right\}$ konstantní rovnou 1, jdou rozdíly k nule, jsou tedy ve stejné tøídì ekvivalence, tedy jsou si rovny.
\end{poznamka}


\end{definice}

\begin{poznamka}
    Jak jsem ji psal, $\mathbb{R}$ se od $\mathbb{Q}$ liší pouze tím, e má kadá shora omezená mnoina supremum. Z~tohoto axiomu plyne, e v $\mathbb{R}$ má \emph{kadá Cauchyovská posloupnost limitu}\footnote{definice Cauchyovskıch posloupností bude dále}, co u $\mathbb{Q}$ neplatí.
\end{poznamka}


\subsection{Pøirozená èísla}

\begin{definice}
	Øekneme, e mnoina $S \subset \mathbb{R}$ je \emph{induktivní}, jestlie platí
	\begin{pitemize}
		\item $1 \in S$
		\item $x \in S \Rightarrow (x+1) \in S$
	\end{pitemize}
	Mnoinu \emph{pøirozenıch èísel $\mathbb{N}$} definujeme jako prùnik všech induktivních podmnoin $\mathbb{R}$, tedy
	$$
		\mathbb{N} := \bigcap \{ S; S \subset \mathbb{R}; \textit{S induktivní}\}
	$$
\end{definice}



\begin{vetaN}{Induktivnost pøirozenıch èísel}
	Mnoina $\mathbb{N}$ je induktivní.
	
	\medskip
	\begin{dukaz}
	    1 je tam proto, e je ve všech. Pokud je nìjaké èíslo $x$ z~$\mathbb{N}$ ve všech, tak i $x+1$ je ve všech.
    \end{dukaz}
\end{vetaN}


\begin{vetaN}{Slabá indukce}
    Pro vlastnost $\varphi$ platí:
	\begin{penumerate}
		\item Platí-li $\varphi(1)$ a $\varphi(n)\Rightarrow\varphi(n+1)$, platí $\varphi$ pro všechny $\mathbb{N}$ \hfill \emph{(slabá indukce)}
	\end{penumerate}
	
	\medskip
	\begin{ideadukazu}
	    Triviálnì z definice.
    \end{ideadukazu}
\end{vetaN}


\begin{vetaN}{Vlastnosti pøirozenıch èísel}
	Mnoina $\mathbb{N}$ má nasledujúcí vlastnosti:
	\begin{penumerate}
		\item $n \in \mathbb{N} \Rightarrow n \ge 1$
		\item $n \in \mathbb{N} \backslash \{1\} \Rightarrow \exists m \in \mathbb{N}: n=m+1$
		\item $m,n \in \mathbb{N}, m < n \Rightarrow m + 1 \le n$ %pøed pravou stranou \exists byt nemusi -- Tuetschek
		\item kadá neprázdná podmnoina $\mathbb{N}$ má nejmenší prvek
		\item Platí-li $\varphi(1)$ a $\left(\forall k \leq n, k \in\mathbb{N} : \varphi(k)\right)\Rightarrow\varphi(n+1)$, platí $\varphi$ pro všechny $\mathbb{N}$ \hfill \emph{(silná indukce)}
	\end{penumerate}
	
	\medskip
	\begin{ideadukazu}
	    5. se dokáe sporem a tím, e \emph{koneèná} mnoina prvkù má vdy nejmenší prvek; 4. se dokáe pøes silnou indukci; 1., 2. a 3. bych dokazoval pøes 4. (ale je moné, e by to byl dùkaz kruhem).
    \end{ideadukazu}
\end{vetaN}

\begin{vetaN}{Archimédova vlastnost reálnıch èísel}
	Pro kadé $x \in \mathbb{R}$ existuje $n \in \mathbb{N}$ takové, e $x < n$.
	
	
	\medskip
	\begin{ideadukazu}
	    Sporem -- vybereme infimum $x$ mnoiny tìch, pro které neexistuje (je zdola omezené 1); pro $x-1$ u musí existovat, take existuje i pro $x$, co vede ke sporu.
    \end{ideadukazu}
\end{vetaN}


\begin{definiceN}{Peanove axiómy pre zavedenie prirodzenıch èísel}
    Opìt mùeme $\mathbb{N}$ zavést jinak. Tato a následující definice jsou ekvivalentní.
    
Mnoina $\mathbb{N}$ je taková mnoina, pro kterou platí:
\begin{pitemize}
	\item Existuje èíslo 0 (to neznamená, e nula je prirodzené èíslo, v $\mathbb{N}$ roli tejto nuly hraje jednotka).
	\item Na mnoine prirodzenıch èísel je definovaná unárna operácia \uv{nasledovník}, oznaèovaná S.
	\item Neexistuje iadne prirodzené èíslo, ktorého nasledovníkom je 0.
	\item Rôzne prirodzené èísla majú rôznych nasledovníkov: $a \neq b \Rightarrow S(a) \neq S(b)$ (t.j. funkcia nasledovníka je prostá).
	\item Ak èíslo 0 spåòa nejakú vlastnos a súèasne ju spåòa kadı nasledovník prirodzeného èísla, potom túto vlastnos spåòajú všetky prirodzené èísla (\emph{axióm matematickej indukcie}).
\end{pitemize}
\end{definiceN}

\begin{definiceN}{Konštrukcia prirodzenıch èísel zaloená na teórii mnoín}
	Oznaème $0 := \{ \}$ a definujme $S(a) = a \cup \{a\}$ pre všetky a. Mnoina prirodzenıch èísel je potom definovaná ako prienik všetkıch mnoín obsahujúcich $0$, ktoré sú uzavreté vzh¾adom na funkciu nasledovníka. Predpokladajúc platnost axiómu nekoneènosti, dá sa dokáza, e táto definícia spåòa Peanove axiómy. \emph{Axióm nekoneènosti} vyzerá takto:
$$\exists\mathbb{N}:\emptyset\in\mathbb{N}\wedge(\forall x:x\in \mathbb{N}\Rightarrow x\cup\{x\}\in\mathbb{N})$$
	
	V \uv{klasickom} zápise èísel potom kadému prirodzenému èíslu zodpovedá mnoina prirodzenıch èísel menších ako ono samo, take
	\begin{pitemize}
		\item $0=\Big\{\Big\}$
		\item $1 = \Big\{0\Big\} = \Big\{\left\{ \right\}\Big\}$
		\item $2 = \Big\{0,1\Big\} = \Big\{0, \left\{0\right\}\Big\} = \Big\{\left\{ \right\}, \left\{\left\{ \right\}\right\}\Big\}$
		\item $3 = \Big\{0,1,2\Big\} = \Big\{0, \left\{0\right\}, \left\{0, \left\{0\right\}\right\}\Big\} = \Big\{\left\{ \right\}, \left\{\left\{ \right\}\right\}, \left\{\left\{ \right\}, \left\{\left\{ \right\}\right\}\right\}\Big\}$
	\end{pitemize}

\end{definiceN}

\begin{poznamka}
$\mathbb{N}$ je uzavretá na sèítanie.
\end{poznamka}

\subsection{Celá a racionální èísla}

\obrazekvpravominipage{matematika/obrazky/05-mnoziny}{Rùzné mnoiny}{fig:mnoziny}{0.2}{0.7}{


\begin{definice}
	Kromì symbolù $\mathbb{R}$ a $\mathbb{N}$, které jsme ji zavedli, budeme znaèit symbolem $\mathbb{Z}$ mnoinu \emph{celıch èísel}, tedy
	$$\mathbb{Z} = \mathbb{N} \cup \{0\} \cup \{-n, n \in \mathbb{N}\}$$
	a symbolem $\mathbb{Q}$ mnoinu \emph{racionálních èísel}, tedy
	$$\mathbb{Q} = \left\{ \frac{p}{q}, p \in \mathbb{Z}, q \in \mathbb{N} \right\}$$
	
	$\mathbb{Z}$ je uzavøena na sèítání, odèítání a násobení,
	$\mathbb{Q}$ je uzavøena na sèítání, odèítání, násobení a dìlení nenulovım èíslem.
	
	Mnoinì $\mathbb{R} \setminus \mathbb{Q}$ se také øíká \emph{iracionální}.
\end{definice}
}

\begin{vetaN}{Existence celé èásti}
	Pro kadé $x \in \mathbb{R}$ existuje právì jedno èíslo $\lfloor x \rfloor \in \mathbb{Z}$ splòující
	$$x-1 < \lfloor x \rfloor \le x$$
	Toto èíslo nazıváme \emph{dolní celou èástí èísla x}.
	
	
	
	\medskip
	\begin{ideadukazu}
	    Sporem -- pro kladné vybereme infimum $x$ mnoiny tìch, pro které neexistuje (je zdola omezené 0); pro $x-1$ u musí existovat, take existuje i pro $x$, co vede ke sporu. Se supremem podobnì.
    \end{ideadukazu}
\end{vetaN}

\begin{vetaN}{Hustota $\mathbb{Q}$ a $\mathbb{R} \backslash \mathbb{Q}$}
	Nech $a,b \in \mathbb{R}, a < b$. Pak existují $q \in \mathbb{Q}$ a $r \in \mathbb{R} \backslash \mathbb{Q}$ takové, e
	$$a < q < b,\ a < r < b$$
	
	\medskip
	\begin{ideadukazu}
	    Vùbec nevím, jestli jde o korektní dùkaz, ale já bych dokazoval \uv{konstrukènì} - pokud jsou $a$ nebo $b$ v $\mathbb{R} \backslash \mathbb{Q}$, vzal bych $q$ jako $(a+b)/2$ a $r$ jako tak dlouhı desetinnı rozvoj $q$, aby $a<r<b$. Pokud jsou $a$ i $b$ v $\mathbb{Q}$, vzal bych $q$ jako $a+\frac{b-a}{\sqrt{2}}$, co by mìlo bıt iracionální èíslo, a pro $r$ udìlal opìt toté. Ale nevím, jestli tøeba existence odmocniny náhodou nezávisí na této vìtì :)
	\end{ideadukazu} 
\end{vetaN}

\begin{vetaN}{o existenci n-té odmocniny}
	Nech $x \in \mathbb{R}, x \ge 0$ a nech $n \in \mathbb{N}$. Pak existuje právì jedno $y \in \mathbb{R}, y \ge 0$ takové, e $y^n = x$.
\end{vetaN}

\subsection{Komplexní èísla}
\begin{definice}
Komplexním èíslem nazveme èíslo tvaru $a + bi$, kde $a,b \in \mathbb{R}$ nazıváme \textbf{reálnou a imaginární èástí} komplexního èísla. Tento tvar komplexního èísla se nazıvá \textbf{algebraickı}. Písmeno $i$ znaèí \textbf{imaginární jednotku}, která se formálnì zavádí jako èíslo splòující rovnici $i^2 +1 = 0$ tj. jako $\sqrt{-1}$ , která v reálnıch èíslech neexistuje.

Pokud je $b = 0$, je dotyèné èíslo reálnım èíslem, tj. reálná èísla tvoøí podmnoinu èísel komplexních. Pokud je $a = 0$, mluvíme o \textbf{ryze imaginárním èísle}.
\end{definice}

\subsubsection{Aritmetika}
Aritmetika je na $\mathbb{C}$ definována následovnì:
\begin{itemize}
\item$(a + ib) + (c+ id) = (a+c) + i(b+d)$
\item$(a + ib) - (c+ id) = (a-c) + i(b-d)$
\item$(a + ib) \cdot (c+ id) = (ac-bd) + i(ad+bc)$

\item$\frac{a+ib}{c+id}  = \frac{(a+ib)(c-id)}{(c+id)(c-id)} = \frac{(ac+bd) + i(bc-ad)}{c^2+d^2} = \left( \frac{ac+bd}{c^2+d^2} \right) + i \left( \frac{bc-ad}{c^2+d^2} \right)$
\end{itemize}

\obrazekvpravo{matematika/obrazky/01-dia1}{Komplexní èíslo 2D}{fig:ComplexNum2D}{0.25}

Pojmem \textbf{komplexnì sdruené èíslo} komplexního èísla $z = a + ib$ se nazıvá èíslo: $$\bar{z} = a - ib$$

\textbf{Absolutní hodnotou} (také \textbf{modul}) komplexního èísla $z = a + bi$ se nazıvá:
$$|z| = \sqrt{a^2 + b^2} = \sqrt{z\cdot \bar{z}}$$

Komplexní èísla se zobrazují v \textbf{komplexní (Gaussovì) rovinì} jako body se souøadnicemi $[x,y]$; $x$ je reálná èást komplexního èísla, $y$ imaginární èást. Na ose $x$ leí reálná èísla, na ose $y$ ryze imaginární èísla.

Následující vlastnosti platí pro všechna komplexní èísla $z$ a $w$, není-li uvedeno jinak.

\begin{itemize}
\item$\overline{z + w} = \overline{z} + \overline{w}$

\item$\overline{zw} = \overline{z}\ \overline{w}$

\item$\overline{\left({\frac{z}{w}}\right)} = \frac{\overline{z}}{\overline{w}},$ pro $w$ nenulové

\item $\overline{z} = z,$ právì kdy je $z$ reálné èíslo

\item $\left| \overline{z} \right| = \left| z \right|$

\item ${\left| z \right|}^2 = z\overline{z}$

\item $z^{-1} = \frac{\overline{z}}{{\left| z \right|}^2},$ pro $z$ nenulové
\end{itemize}


Kadé komplexní èíslo $z$ rùzné od nuly je moné jednoznaènì vyjádøit v \textbf{goniometrickém tvaru}. 


Pokud si v komplexní rovinì zvolíme polární souøadnicovı systém, vzdálenost èísla $z$ od poèátku je právì jeho absolutní hodnota $|z|$ a orientovanı úhel $\varphi = |\sphericalangle JOZ|$ (\emph{argument}), kde $J$ je bod $J[1,0]$, $O$ je poèátkem soustavy a $Z$ je obraz komplexního èísla $z=a + bi$ se souøadnicemi $Z[a,b]$, platí:
$$z = \left|z\right| \left(\cos \varphi + i\cdot \sin \varphi\right)$$

Argument $\varphi$ lze vyjádøit ze vztahù: $\cos \varphi = \frac{a}{|z|}$ a $\sin \varphi = \frac{b}{|z|}$

Pro dìlení komplexních èísel $z_1 = |z_1|\cdot (\cos\varphi_1 + i\cdot \sin
\varphi_1)$ a $z_2 = |z_2|\cdot (\cos \varphi_2 + i\cdot \sin \varphi_2)$ platí následující rovnice:
$$\frac{z_1}{z_2} = \frac{|z_1|}{|z_2|}\cdot \left( \cos(\varphi_1 - \varphi_2) + i\cdot \sin(\varphi_1 - \varphi_2) \right)$$

Pro násobení komplexních èísel $z_1$ a $z_2$ z~pøedchozího pøíkladu slouí vzorec:
$$z_1 \cdot  z_2 = |z_1|\cdot |z_2|\cdot \left( \cos(\varphi_1 + \varphi_2) + i\cdot \sin(\varphi_1 + \varphi_2) \right)$$

Pro \textbf{n-tou mocninu komplexní èísla} v goniometrickém tvaru platí tzv. \textbf{Moivreova vìta}:
$$z^n = |z|^n (\cos n \varphi + i\cdot \sin n \varphi)$$

Odmocnina není v $\mathbb{C}$ jednoznaèná -- napø. $i^2 = (-i)^2 = i$, nebo napø. $i^3 = \left(-\frac{\sqrt{3}}{2}-\frac{i}{2}
\right)^3 = \left(\frac{\sqrt{3}}{2}-\frac{i}{2}
\right)^3 = -i$.

Platí, e kadı polynom má v~$\mathbb{C}$ koøen.

\subsection{Posloupnosti a limity}
\begin{definiceN}{posloupnost}
\emph{Posloupností reálnıch èísel} nazıváme jakékoli zobrazení mnoiny $\mathbb{N}$ do mnoiny $\mathbb{R}$. Posloupnost obvykle znaèíme symbolem $\{a_n\}^{\infty}_{n=1}$ nebo $\{a_n\}_{n \in \mathbb{N}}$. Pro kadé konkrétní $n \in \mathbb{N}$ nazıváme reálné èíslo $a_n$ \emph{n-tım èlenem} posloupnosti $\{a_n\}$.
\end{definiceN}

\begin{definiceN}{Omezené posloupnosti}
\begin{penumerate}
	\item Posloupnost $\{a_n\}$ je \emph{shora omezená}, je-li $\{a_n; n \in \mathbb{N}\}$ shora omezená.
	\item Posloupnost $\{a_n\}$ je \emph{zdola omezená}, je-li $\{a_n; n \in \mathbb{N}\}$ zdola omezená.
	\item Posloupnost $\{a_n\}$ je \emph{omezená}, je-li zdola omezená a shora omezená.
\end{penumerate}
\end{definiceN}

\begin{definiceN}{Rostoucí a klesající posloupnosti}
\begin{penumerate}
	\item Posloupnost $\{a_n\}$ je \emph{klesající}, jestlie $\forall n \in \mathbb{N}: a_n > a_{n+1}$.
	\item Posloupnost $\{a_n\}$ je \emph{rostoucí}, jestlie $\forall n \in \mathbb{N}: a_n < a_{n+1}$.
	\item Posloupnost $\{a_n\}$ je \emph{neklesající}, jestlie $\forall n \in \mathbb{N}: a_n \le a_{n+1}$.
	\item Posloupnost $\{a_n\}$ je \emph{nerostoucí}, jestlie $\forall n \in \mathbb{N}: a_n \ge a_{n+1}$.
	\item Posloupnost $\{a_n\}$ je \emph{monotónní}, jestlie je nerostoucí nebo neklesající.
	\item Posloupnost $\{a_n\}$ je \emph{ryze monotónní}, jestlie je rostoucí nebo klesající.
\end{penumerate}
\end{definiceN}


\subsubsection{Vlastní limity}




\begin{definice}
    
Nech $\{a_n\}$ je posloupnost reálnıch èísel a $A \in \mathbb{R}$. Øekneme, e A je \emph{vlastní limitou posloupnosti} $\{a_n\}$, jestlie
$$\forall \varepsilon > 0\ \exists n_0 \in \mathbb{N}: \forall n \ge n_0, n \in \mathbb{N}: |a_n - A| < \varepsilon$$
znaèíme
$$\lim_{n \rightarrow \infty} a_n = A$$
\end{definice}

\obrazekvpravo{matematika/obrazky/02-limita}{Ilustrace k definici vlastní limity}{fig:limita_posloupnosti}{0.5}




\begin{poznamka}
Na obrázku \ref{fig:limita_posloupnosti} jsem nakreslil posloupnost k osvìtlení definice limity. Jde o posloupnost\footnote{konkrétnì $a_0=1, a_n=\cos a_{n-1}$, ale není to dùleité}, která konverguje k èíslu $0.739085$, oznaèeno $A$. 

\end{poznamka}


\begin{definice}
Jestlie existuje $A \in \mathbb{R}$ tak, e $\lim_{n \rightarrow \infty}a_n = A$, pak øíkáme, e posloupnost $\{a_n\}$ má \emph{vlastní limitu} nebo e \emph{konverguje} (je \emph{konvergentní}). V opaèném pøípadì øíkáme, e posloupnost \emph{diverguje}.
\end{definice}

\begin{pozorovani}
Ne kadá posloupnost je konvergentní. Napøíklad posloupnost 0,1,0,1,0,... nemá vlastní limitu a podobnì posloupnost $\{2^n\}$ nemá vlastní limitu.
\end{pozorovani}

\begin{priklady}
\begin{pitemize}
	\item $\lim_{n \rightarrow \infty} (\sqrt{n+1} - \sqrt{n}) = 0$
	\item $\lim_{n \rightarrow \infty} \sqrt[n]{n} = 1$
\end{pitemize}
\end{priklady}

\begin{poznamka}
Následující vìty mají pomìrnì lehké dùkazy, které zde jenom naznaèím; pokud si je budete zkoušet, je dobré si u nich uvìdomit základní vlastnosti absolutní hodnoty:
\begin{pitemize}
	\item $\left|a \cdot b\right|= \left|a\right| \cdot\left| b\right|$
	\item $\left|a + b\right| \leq \left|a\right| + \left| b\right|$
	\item $\left|a \right| \leq b\Leftrightarrow -b \leq a \leq b$
\end{pitemize}
\end{poznamka}

\begin{vetaN}{o jednoznaènosti limity posloupnosti}
Kadá posloupnost má nejvıše jednu limitu.


\medskip
\begin{ideadukazu}
    Kdyby mìla dvì, zvolíme $\varepsilon$ menší ne vzdálenost. Od vìtšího z dvou $n_0$ (jedno pro kadou z~limit) by pak musela bıt zároveò ve dvou disjunktních intervalech -- spor.
\end{ideadukazu}


\end{vetaN}

\begin{vetaN}{o omezenosti konvergentní posloupnosti}
Kadá konvergentní posloupnost je omezená.


\medskip
\begin{ideadukazu}
    Pro libovolnì zvolené $\varepsilon$ je od nìjakého $n_0$ posloupnost omezená $A+\varepsilon$ shora a $A-\varepsilon$ zdola a pøed $n_0$ je jich koneènì mnoho, take mùeme vybrat nejvìtší/nejmenší.
\end{ideadukazu}

\end{vetaN}

\begin{definice}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel. Øekneme, e posloupnost $\{b_k\}_{k \in \mathbb{N}}$ je \emph{vybraná} z~posloupnosti, neboli e posloupnost $\{b_k\}_{k \in \mathbb{N}}$ je \emph{podposloupností} posloupnosti $\{a_n\}_{n \in \mathbb{N}}$, jestlie existuje rostoucí posloupnost pøirozenıch èísel $\{n_k\}$ taková, e $b_k = a_{n_k}$ pro všechna $k \in \mathbb{N}$.
\end{definice}

\begin{poznamka}
    Tahle definice je trochu neprùhledná, ale jenom øíká, e z~jedné posloupnosti vybereme jinou.
\end{poznamka}

\begin{vetaN}{o limitì vybrané posloupnosti}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel a nech $\lim_{n \rightarrow \infty} a_n = A$. Nech posloupnost $\{b_k\}_{k \in \mathbb{N}}$ je vybraná z~posloupnosti $\{a_n\}_{n \in \mathbb{N}}$. Pak $\lim_{k \rightarrow \infty} b_k = A$.

\medskip
\begin{ideadukazu}
    Nemùe jít jinam -- pokud je $n_0$ u $a_n$ v~intervalu, tím spíš pak bude $b_n$ v intervalu.
\end{ideadukazu}

\end{vetaN}

\begin{vetaN}{o aritmetice limit}
Nech $\{a_n\}_{n \in \mathbb{N}}$ a $\{b_n\}_{n \in \mathbb{N}}$ jsou dvì posloupnosti reálnıch èísel a nech $\lim_{n \rightarrow \infty} a_n = A \in \mathbb{R}$ a $\lim_{n \rightarrow \infty} b_n = B \in \mathbb{R}$. Pak platí:
\begin{enumerate}
	\item $\lim_{n \rightarrow \infty} (a_n+b_n) = A+B$
	\item $\lim_{n \rightarrow \infty} (a_n \cdot  b_n) = A\cdot B$
	\item je-li $\forall n \in \mathbb{N}: b_n \neq 0$ a $B \neq 0$, pak $\lim_{n \rightarrow \infty} \frac{a_n}{b_n}=\frac{A}{B}$
\end{enumerate}


\medskip
\begin{ideadukazu}
    \begin{enumerate}
    \item Pro kadé $\varepsilon$ mùeme vzít $\frac{\varepsilon}{2}$, pro $a$ i $b$ najít jejich $n_0$, od vìtšího z nich je i souèet ve $\varepsilon$
    
    \item Víceménì toté, vezmeme $\sqrt{\varepsilon}$
    
    \item Dokáeme, e $\frac{1}{b_n}$ jde k $\frac{1}{B}$. Jednoduchou úpravou máme $$\left|\frac{1}{b_n}-\frac{1}{B}\right| = \left|\frac{1}{b_n}\right|\cdot\left|\frac{1}{B}\right|\cdot\left|b_n-B\right|.$$ 
    
    K omezení $\left|\frac{1}{b_n}\right|<\left|\frac{2}{B}\right|$ provedeme dost zbìsilı trik\footnote{z Drahošovıch skript; pokud to nìkdo umíte líp, tak napište na fórum, ale pochybuji}. 
    
    $$\Big|\left|b_n\right| - \left|B\right|\Big| \leq \left|b_n-B\right|<\varepsilon,$$ take $\left|b_n\right|\geq\left|B\right|-\varepsilon$.
    
    Pokud bychom vzali $\varepsilon<|B|$, je $\frac{1}{\left|B\right|-\varepsilon}>0$ a proto $$\left|\frac{1}{b_n}\right|\cdot\left|\frac{1}{B}\right|\cdot\left|b_n-B\right| \leq \left|\frac{1}{\left|B\right|-\varepsilon}\right|\cdot\left|\frac{1}{B}\right|\cdot\left|b_n-B\right|.$$
    
    Pokud bychom vzali $\varepsilon<\frac{\left|B\right|}{2}$, je $$\frac{1}{\left|B\right|-\varepsilon}<\frac{1}{\left|B\right|-\frac{\left|B\right|}{2}}=\frac{2}{\left|B\right|},$$ take celı rozdíl pùvodních zlomkù $$\left|\frac{1}{b_n}-\frac{1}{B}\right| < \left|\frac{2}{B}\right|\cdot\left|\frac{1}{B}\right|\cdot\left|b_n-B\right|.$$
    
    Dále jasné, protoe $\left|\frac{2}{B}\right|\cdot\left|\frac{1}{B}\right|$ je konstantní.
    
    \end{enumerate}
\end{ideadukazu}



\end{vetaN}

\begin{vetaN}{o limitì a uspoøádání}
Nech $\{a_n\}_{n \in \mathbb{N}}$ a $\{b_n\}_{n \in \mathbb{N}}$ jsou dvì posloupnosti reálnıch èísel a nech $\lim_{n \rightarrow \infty} a_n = A \in \mathbb{R}$ a $\lim_{n \rightarrow \infty} b_n = B \in \mathbb{R}$. Pak platí:
\begin{penumerate}
	\item Jestlie $A < B$, potom $\exists n_0 \in \mathbb{N}\ \forall n > n_0: a_n < b_n$
	\item Jestlie $\exists n_0 \in \mathbb{N}\ \forall n \ge n_0: a_n \ge b_n$, pak $A \ge B$
\end{penumerate}

\noindent Pozor, ostrost nerovností v tomto pøípadì je dùleitá.

\medskip
\begin{ideadukazu}
    Provede se sporem a \uv{chytøe} velkımi epsilony.
\end{ideadukazu}
\end{vetaN}

\begin{vetaN}{o policajtech}
Nech $\{a_n\}_{n \in \mathbb{N}}$, $\{b_n\}_{n \in \mathbb{N}}$ a $\{c_n\}_{n \in \mathbb{N}}$ jsou posloupnosti reálnıch èísel, splòující
\begin{penumerate}
	\item $\exists n_0 \in \mathbb{N}\ \forall n > n_0: a_n \le c_n \le b_n$
	\item $\lim_{n \rightarrow \infty}a_n = \lim b_n = A \in \mathbb{R}$
\end{penumerate}
Pak
$$
\lim c_n = A
$$


\medskip
\begin{ideadukazu}
    Pokud je v epsilonu v jedné i v druhé, zvolíme vìtší z $n_0$ a musí tam bıt i ta prostøední.
\end{ideadukazu}

\end{vetaN}

\begin{vetaN}{o limitì souèinu mizející ($\lim=0$) a omezené posloupnosti}
Nech $\{a_n\}_{n \in \mathbb{N}}$ a $\{b_n\}_{n \in \mathbb{N}}$ jsou posloupnosti reálnıch èísel, nech je $\lim_{n \rightarrow \infty} a_n = 0$ a $\{b_n\}$ omezená. Pak
$$
\lim_{n \rightarrow \infty}(a_n \cdot b_n) = 0
$$


\medskip
\begin{ideadukazu}
    U mizející vezmeme $\frac{\varepsilon}{K}$, kde $K$ je horní omezení $b_n$.
\end{ideadukazu}

\end{vetaN}

\subsubsection{Nevlastní limity}
\begin{definice}
Øekneme, e posloupnost  $\{a_n\}$ má \emph{nevlastní limitu} $+ \infty$, jestlie:
$$ \forall K \in \mathbb{R}\ \exists n_0 \in \mathbb{N}: \forall n \ge n_0, n \in \mathbb{N}: a_n \ge K $$

Obdobnì øekneme, e posloupnost  $\{a_n\}$ má \emph{nevlastní limitu} $- \infty$, jestlie:
$$ \forall K \in \mathbb{R}\ \exists n_0 \in \mathbb{N}: \forall n \ge n_0, n \in \mathbb{N}: a_n \le K $$

Má-li posloupnost nevlastní limitu, øíkáme o ní, e diverguje, stejnì jako v pøípadì, e ádnou limitu nemá.
\end{definice}


\obrazekvpravominipage{matematika/obrazky/03-limita_nevlastni}{Ilustrace k definici nevlastní limity}{fig:limita_nevlastni}{0.5}{0.5} {

\begin{poznamka}
Ilustrace k definici nevlastní limity je na obrázku \ref{fig:limita_nevlastni}.
\end{poznamka}


\begin{poznamka}
Všechny moné situace jsou:

\noindent Limita posloupnosti:


\begin{pitemize}
	\item neexistuje
	\item existuje
	\begin{pitemize}
		\item vlastní (= posloupnost konverguje)
		\item nevlastní
		\begin{pitemize}
			\item $- \infty$
			\item $+ \infty$
		\end{pitemize}
	\end{pitemize}
\end{pitemize}

\end{poznamka}
}


\begin{definice}
Mnoinu $\mathbb{R}^* := \mathbb{R} \cup \{ + \infty, - \infty \}$ nazıváme \emph{rozšíøenou reálnou osou}.
\end{definice}

\begin{poznamka}
Vìty o jednoznaènosti limity, o limitì vybrané posloupnosti, o limitì a uspoøádání a o policajtech platí v nezmìnìné podobì, jestlie pøipustíme nevlastní limity. Vìta o omezenosti konvergentní posloupnosti zrejmì neplatí - nebo je-li $\lim_{n \rightarrow \infty} a_n=\infty$ (nebo $- \infty$), pak posloupnost $\{a_n\}$ není omezená; je ale omezená \emph{zdola} u $\infty$ a \emph{shora} u $- \infty$. Vìtu o aritmetice limit pro rozšírenou osu uvedeme zvláš.
\end{poznamka}

\begin{vetaN}{o aritmetice limit pro nevlastní limity}
Nech $\{a_n\}_{n \in \mathbb{N}}$ a $\{b_n\}_{n \in \mathbb{N}}$ jsou dvì posloupnosti reálnıch èísel a nech $\lim_{n \rightarrow \infty} a_n = A \in \mathbb{R}^*$ a $\lim_{n \rightarrow \infty} b_n = B \in \mathbb{R}^*$. Pak platí:
\begin{penumerate}
	\item $\lim_{n \rightarrow \infty} (a_n+b_n) = A+B \textit{, pokud je vıraz A+B definován}$
	\item $\lim_{n \rightarrow \infty} (a_n \cdot  b_n) = A\cdot B\textit{, pokud je vıraz }A\cdot B\textit{ definován}$
	\item je-li $\forall n \in \mathbb{N}: b_n \neq 0$ a $B \neq 0$, pak $\lim_{n \rightarrow \infty} \frac{a_n}{b_n}=\frac{A}{B} \textit{, pokud je vıraz } \frac{A}{B} \textit{ definován}$
\end{penumerate}


\medskip
\begin{ideadukazu}

\begin{penumerate}
	\item Pokud je $\left\{a_n\right\}$ v $\mathbb{R}$, je zdola omezená $L$, budeme brát (pokud $\left\{b_n\right\}$ jde k~$\infty$) $K-L$, podobnì u dalších variant.
	\item Tøeba $\left\{a_n\right\}$ jde k~$A$ v $\mathbb{R}$ a $A>0$ a $\left\{b_n\right\}$ jde k~$\infty$, pro ostatní se dokáe obdobnì. Pro $\alpha>0$ udìláme $\epsilon = A-\alpha$, z~toho plyne $a>\alpha$; $b_n$ urèitì jde nad $\frac{L}{\alpha}$ pro libovolné $L$ a tedy $a_nb_n>L$.
	
	\item Dokáeme, e kdy $b_n$ jde k $\infty$ tak $\frac{1}{b_n}$ jde k~$0$ -- pro $\epsilon$ platí odnìkud $b_n>\frac{1}{\epsilon}$, ale je toté, co $\frac{1}{b_n}<\varepsilon$.
	
\end{penumerate}
\end{ideadukazu}

\end{vetaN}

\begin{definiceN}{Supremum a infimum na rozšíøené reálné ose}
\begin{pitemize}
	\item Nech mnoina $A \subset \mathbb{R}$ je shora neomezená. Pak klademe $\sup A := +\infty$
	\item Nech mnoina $A \subset \mathbb{R}$ je zdola neomezená. Pak klademe $\inf A := -\infty$
	\item Nech $A=\emptyset$. Pak klademe $\sup A := -\infty$ a $\inf A := +\infty$
\end{pitemize}
\end{definiceN}

\begin{poznamka}
Prázdná mnoina je jediná mnoina, její supremum je menší ne její infimum.
\end{poznamka}

\begin{vetaN}{o limitì podílu kladné a mizející posloupnosti}
Nech $\{a_n\}_{n \in \mathbb{N}}$ a $\{b_n\}_{n \in \mathbb{N}}$ jsou posloupnosti reálnıch èísel, nech je $\lim_{n \rightarrow \infty} a_n = A \in \mathbb{R}^*, A>0$ a nech $\lim_{n \rightarrow \infty}\{b_n\}=0$. Nech
$$
	\exists n_0 \in \mathbb{N} \textit{ } \forall n \ge n_0, n \in \mathbb{N}: b_n>0
$$
Pak
$$
	\lim_{n \rightarrow \infty} \frac{a_n}{b_n} = + \infty
$$
\medskip
\begin{ideadukazu}
Jestlie $K$ je horní omezení, pak mùeme vzít epsilon u mizející $\frac{1}{K}$, souèin pak jde nad $K$.
	
\end{ideadukazu}

\end{vetaN}

\subsubsection{Monotónní posloupnosti}
\begin{vetaN}{o limitì monotónní posloupnosti}
Kadá monotónní posloupnost má limitu.

\medskip\begin{ideadukazu}
Napø. rostoucí posloupnost buï není shora omezená a má tedy limitu $\infty$, nebo je shora omezená, protoe je ale zároveò rostoucí, tak se k danému hornímu omezení postupnì pøibliuje.
\end{ideadukazu}

\end{vetaN}

\begin{poznamka}
Je-li posloupnost neklesající (nerostoucí) a navíc shora (zdola) omezená, pak má vlastní limitu. Tato limita se navíc rovná supremu (infimu) hodnot.
Je-li posloupnost neklesající (nerostoucí) a navíc shora (zdola) neomezená, pak má limitu $+ \infty$ ($- \infty$).
\end{poznamka}

\begin{definiceN}{Limes superior a limes inferior}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel. Je-li $\{a_n\}$ shora omezená, definujeme posloupnost $\{b_n\}_{n \in \mathbb{N}}$ pøedpisem:
$$
	b_n := \sup\{a_k; k \ge n \}
$$

$b_n$ \emph{neroste}.

Je-li $\{a_n\}$ zdola omezená, definujeme posloupnost $\{c_n\}_{n \in \mathbb{N}}$ pøedpisem:
$$
	c_n := \inf\{a_k; k \ge n \}
$$

$c_n$ \emph{nekles‡}.

V takovém pøípadì definujeme:
$$
\lim \sup~a_n := \left\{
\begin{array}{ll} \lim_{n \rightarrow \infty} b_n & \textit{jestlie je } \{a_n\} \textit{ shora omezená} \\ \infty & \textit{jestlie je } \{a_n\} \textit{ shora neomezená} \end{array}
\right.
$$
Tuto hodnotu nazıváme \emph{limes superior} posloupnosti $\{a_n\}_{n \in \mathbb{N}}$. Obdobnì definujeme \emph{limes inferior} posloupnosti $\{a_n\}_{n \in \mathbb{N}}$ pøedpisem:
$$
\lim \inf~a_n := \left\{
\begin{array}{ll} \lim_{n \rightarrow \infty} c_n & \textit{jestlie je } \{a_n\} \textit{ zdola omezená} \\ - \infty & \textit{jestlie je } \{a_n\} \textit{ zdola neomezená} \end{array}
\right.
$$
\end{definiceN}

\begin{poznamka}
Limes superior a limes inferior jsou vdy dobøe definované hodnoty a platí
$$
\lim \sup~a_n \in \mathbb{R}^*, \lim \inf~a_n \in \mathbb{R}^*, 
$$
Na rozdíl od limity, která nemusí existovat, tyto dvì hodnoty existují pro
libovolnou posloupnost reálnıch èísel.
\end{poznamka}

\begin{poznamka}
    Limes superior a limes inferior jsou nìkdy definovány jako $\inf \sup$ a $\sup \inf$ -- jeliko jde o monotónní posloupnosti, jde ale o toté.
\end{poznamka}

\begin{vetaN}{o vztahu limity, limes superior a limes inferior}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel. Potom 
$$
	\lim a_n = A \in \mathbb{R}^{*}
$$
právì tehdy, kdy
$$
\lim \sup~a_n = \lim \inf~a_n = A \in \mathbb{R}^*
$$

\medskip\begin{dukaz}
Pro vlastní limity:

$\Rightarrow$ : Nech $\lim_n a_n = A$, zvolme libovolné $\varepsilon > 0$. Zvolme $n_0>0$ takové, e pro $n\geq n_0$ je $A-\varepsilon < a_n < A + \varepsilon$. Tedy pro $n \geq n_0$ je $A-\varepsilon \leq \inf_{k \geq n}a_n < A + \varepsilon$. Jeliko posloupnost $\left\{ \left(\inf_{k\geq n}a_k\right)_n\right\}$ neklesá a je shora omezená, musí limita existovat a musí platit $\lim_{n \rightarrow \infty} \leq A+\varepsilon$. 

Protoe $\varepsilon$ je nekoneènì malé, platí $\lim \inf a_n = A$. 

Podobnì pro $\lim \sup.$

$\Leftarrow$ : Nech $\lim \sup~a_n = \lim \inf~a_n = A$. Potom pro kadé $\varepsilon >0$existuje $n_1>0$ takové, e pro $n\geq n_1$ je $A-\varepsilon < \sup_{k\geq n}a_k < A+\varepsilon$. Také existuje $n_2>0$ takové, e pro $n\geq n_2$ je $A-\varepsilon < \inf_{k\geq n}a_k < A+\varepsilon$. Vezmeme $n_0=\max\left(n_1,n_2\right)$; pro $n\geq n_0$ je $\sup_{k\geq n}a_k$ i $\inf_{k\geq n}a_k$ mezi $A-\varepsilon$ a $A\varepsilon$. Ale protoe je v tomto intervalu suprémum i infimum, musí v tomto intervalu bıt i všechny hodnoty $a_n \forall n \geq n_0$.

\noindent Pro nevlastní limity:

Dokáeme pro $\infty$, pro $-\infty$ se dokáe obdobnì.

$\Rightarrow$: Zvolme $K$ libovolnì. Potom je-li $ \lim \inf~a_n = \infty,$ je pro nìjaké $n$ $\inf_{k\geq n}a_k > K$, tedy $\forall k\geq n$ je $a_k > K$.

$\Leftarrow$: Je-li $ \lim~a_n = \infty$, pro dostateènì velké $n$ platí, e $k\geq n \Rightarrow a_k>K$, tedy $\inf_{k\geq n}a_k > K$ a $\sup_{k\geq n}a_k > K$.

\end{dukaz}


\end{vetaN}

\begin{definice}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel. Pak $A \in \mathbb{R}^*$ nazveme \emph{hromadnou hodnotou} posloupnosti $\{a_n\}$, jestlie existuje vybraná posloupnost taková $\{a_{n_k}\}$, e $\lim_{k \rightarrow \infty} a_{n_k} = A$. Mnoina všech hromadnıch hodnot znaèíme $H(\{a_n\})$
\end{definice}

\begin{vetaN}{o vztahu limes superior, limes inferior a hromadnıch hodnot}
Nech $\{a_n\}_{n \in \mathbb{N}}$ je posloupnost reálnıch èísel. Potom $H\{(a_n)\} \neq \emptyset$,
$$
\lim \sup~a_n = \max H(\{a_n\}) \textit{ a } \lim \inf~a_n = \min H(\{a_n\})
$$
\end{vetaN}

K tomuhle jsem dùkaz nenašel ani nevymyslel :-(

\subsection{Cauchyovské posloupnosti}

Tato sekce je vypracovaná podle skript Prof. A. Pultra z~matematické analızy (\url{http://kam.mff.cuni.cz/~pultr/}).
\bigskip



\begin{vetaN}{Bolzano-Weierstrassova}
Z kadé omezené posloupnosti lze vybrat konvergentní podposloupnost. Jsou-li $a_n$ v kompaktním intervalu $[a,b]$, je limita vybrané posloupnosti v tomto intervalu.

\medskip\begin{dukaz}
První èást dokáeme nalezením takové posloupnosti. 

Vezmeme $A:=\lim\sup~a_n$ -- to mùeme, protoe $\left\{a_n\right\}$ je shora omezená.

Definujeme pro kadé $k\in\mathbb{N}$ mnoinu $M_k:=\{j\in\mathbb{N}|j>n_{k-1},a_j\in\langle A - \frac{1}{2^k}, A + \frac{1}{2^k}\rangle\}$ a $n_k:=\min M_k$ -- pro kadé $k$ taková mnoina je, protoe $A$ je limita suprem, tj. pro libovolnì malé okolí A jsou od nìjakého bodu dál všechna suprema pouze v nìm. 

Potom $\{a_{n_k}\}$ je vybraná posloupnost, která konverguje. 

\noindent Druhá èást je pøímım dùsledkem vìty o limitì a uspoøádání -- limita \uv{nemùe} bıt jinde.
\end{dukaz}
\end{vetaN}


\begin{definiceN}{Bolzano-Cauchyova podmínka}
Øekneme, e posloupnost $\{a_n\}_{n\geq 0}$ je \emph{cauchyovská}, nebo-li e splòuje \emph{Bolzano-Cauchyho podmínku}, pokud pro ní platí:
$$
\forall \varepsilon > 0~~\exists n_0 \in \mathbb{N}: \forall m, n \in \mathbb{N}: m \ge n_0, n \ge n_0: |a_n - a_m| < \varepsilon
$$
\end{definiceN}



\begin{lemma}
Má-li cauchyovská posloupnost konvergentní podposloupnost, je konvergentní.

\medskip\begin{dukaz}
Nech $\lim a_{k_n} = x$. Pro $\varepsilon > 0$ zvolme $n_0$, aby pro $m,n\geq n_0$ platilo $|a_m-a_n|<\frac{\varepsilon}{2}$ a $|a_{k_n}-x|<\frac{\varepsilon}{2}$. Protoe $k_n\geq n$, platí $|a_n-x|=|a_n-a_{k_n}|+|a_{k_n}-x|<\varepsilon$.
\end{dukaz}
\end{lemma}


\begin{vetaN}{Bolzano-Cauchyova}
Posloupnost $\{a_n\}$ má vlastní limitu, právì kdy je cauchyovská.

\medskip\begin{dukaz}
\begin{enumerate}
    \item Implikace \uv{$\Rightarrow$} je hned vidìt -- staèí vzít k $\varepsilon$ takové $n_0$, e $|a_n-x|<\frac{\varepsilon}{2}\ \forall n\geq n_0$. Potom je $|a_n-a_m|=|a_n-x+x-a_m|\leq|a_n-x|+|x-a_m|\leq\varepsilon\ \forall m,n\geq n_0$.
    
    Øeèeno èesky -- v pásmu velkém $\varepsilon$ jsou si všichni vzdáleni maximálnì $\varepsilon$
    
    \item Pro druhou implikaci staèí dokázat, e je cauchyovská posloupnost omezená a zbytek dostaneme z~pøedchozího lemmatu a Bolzano-Weierstrassovy vìty. 
    
    Pro $\varepsilon=1$ existuje $n_0$ takové, e $a_{n_0}-1<a_n<a_{n_0}+1$ pro kadé $n\geq n_0$ (to plyne pøímo z~podmínky), take zbıvá jen koneènı poèet èlenù mimo toto rozmezí (pro $n<n_0$), a ty vdy tvoøí omezenı systém, tedy posloupnost je omezená.
\end{enumerate}
\end{dukaz}
\end{vetaN}

