\def\sgn{\mathrm{sgn}}
\def\adj{\mathrm{adj\ }}

\section{Determinanty}

\begin{e}{Poadavky}{0}{0}
\begin{pitemize}
\item Definice a základní vlastnosti determinantu
\item Úpravy determinantù, vıpoèet
\item Geometrickı smysl determinantu
\item Minory a inversní matice
\item Cramerovo pravidlo.
\end{pitemize}
\end{e}

\subsection{Definice a základní vlastnosti determinantu}

\begin{obecne}{Neformálnì}
V lineární algebøe je \emph{determinant} zobrazení, které pøiøadí kadé ètvercové matici $A$ skalár $\det A$.

Determinantem ètvercové matice øádu $n$ nazıváme souèet všech souèinù $n$ prvkù této matice takovıch, e v ádném z uvedenıch souèinù se nevyskytují dva prvky z tého øádku ani z tého sloupce. Kadı souèin pøitom násobíme èísly $r$ a $s$, kde $r$ pøedstavuje znaménko permutace pøíslušného poøadí prvních indexù a $s$ znaménko permutace pøíslušného poøadí druhıch indexù.
\end{obecne}

\begin{e}{Definice}{0}{permutace, znaménko}
\emph{Permutace} je libovolná bijekce $\sigma:X\to X$. Mnoina \emph{inversí} nìjaké permutace $\sigma$ je $I(\sigma)=\{(i,j):i<j \ \&\ \sigma(i)>\sigma(j)\}$. Znaménko permutace $\sgn(\sigma)$ se pak definuje jako $\sgn(\sigma)=(-1)^{|I(\sigma)|}$. Nejjednodušší permutace -- zámìna dvou prvkù -- se pak nazıvá \emph{transpozice}. Ta má vdy znaménko $-1$ a libovolná permutace je sloením nìjakıch transpozic. Mnoinu všech permutací na mnoinì $\{1,\dots,n\}$ znaèíme $S_n$.
\end{e}

\begin{e}{Poznámka}{0}{0}
Pro skládání permutací platí $\sgn(p\circ q)=\sgn(p)\sgn(q)$. Pro inversní permutaci (inversní zobrazení) platí $\sgn(p)=\sgn(p^{-1})$.
\end{e}

\begin{e}{Definice}{0}{determinant}
Nech $A = (a_{i,j})^{n}_{i,j=1}$ je ètvercová matice øádu $n$.
Determinant je definovanı pomocí \emph{Leibnizova vzorce}:

$$\det A = \sum_{\sigma \in S_n}sgn(\sigma) \prod_{i=1}^n a_{i,\sigma(i)}$$
\end{e}

\begin{e}{Poznámka}{0}{0}
Suma se poèítá pøes všechny permutace $\sigma$ èísel \{1,2,\dots,n\}, take tento vzorec obsahuje $n!$ (faktoriál) sèítancù, co jej s rùstem \emph{n} rychle èiní prakticky nepouitelnım pro vıpoèet. V praxi se proto pouívají jiné zpùsoby vıpoètu.
\end{e}

\begin{e}{Poznámka}{0}{0}
Konkrétnì, pro matici øádu $n$, kde:
\begin{pitemize}
\item $n=1: \det A = a_{1,1}$
\item $n=2: \det A = a_{1,1} a_{2,2} - a_{2,1} a_{1,2}$
\item $n=3: \det A = a_{1,1} a_{2,2} a_{3,3} + a_{1,3} a_{2,1} a_{3,2} + a_{1,2} a_{2,3} a_{3,1} - a_{1,3} a_{2,2} a_{3,1} - a_{1,1} a_{2,3} a_{3,2} - a_{1,2} a_{2,1} a_{3,3}$
\end{pitemize}

Mnemotechnická pomùcka slouící k zapamatování postupu vıpoètu determinantu tøetího øádu se nazıvá \emph{Sarrusovo pravidlo}:
\begin{center} \includegraphics[width=5cm]{matematika/obrazky/sarrusovo_pravidlo.png} \end{center}
\end{e}

\begin{e}{Poznámka}{0}{0}
Obecnı vzorec lze také vyjádøit pomocí Levi-Civitova symbolu $\epsilon_{{j_1}{j_2}\dots{j_n}}$ jako
$$\det A = \sum_{{j_1},{j_2}, \dots ,{j_n}}\epsilon_{{j_1}{j_2} \dots {j_n}} a_{1,j_1}a_{2,j_2}  \dots  a_{n,j_n} = \sum_{{j_1},{j_2}, \dots ,{j_n}}\epsilon_{{j_1}{j_2} \dots {j_n}} a_{j_1,1}a_{j_2,2}  \dots  a_{j_n,n}$$
\end{e}

\subsubsection*{Vlastnosti determinantu}

\begin{e}{Vìta}{0}{O determinantu transponované matice}
Pro ètvercovou matici $A$ øádu $n$ platí:
$$\det A=\det A^T$$

\begin{e}{Dùkaz}{0}{0}
Plyne z faktu e $\sgn(p)=\sgn(p^{-1})$:
\begin{align*}
\det(A^T) & =\sum_{p\in S_n}\sgn(p)\prod_{i=1}^n(A^T)_{i,p(i)}=\\
 & =\sum_{p\in S_n}\sgn(p)\prod_{i=1}^n a_{p(i),i}=\sum_{p^{-1}\in S_n}\sgn(p^{-1})\prod_{i=1}^n a_{i,p^{-1}(i)}=\det(A)
\end{align*}
\end{e}
\end{e}

\begin{e}{Vìta}{0}{Pøerovnání matice}
Pøerovnání øádkù nebo sloupcù podle permutace $p$ nezmìní determinant vùbec, pokud $\sgn(p)=1$ a zmìní jen jeho znaménko, pokud $\sgn(p)=-1$.

\begin{e}{Dùkaz}{0}{0}
A buï pùvodní matice a B pøerovnaná:
\begin{align*}
\det(B) & =\sum_{p\in S_n}\sgn(p)\prod_{i=1}^n(B)_{i,p(i)} = \sum_{p\in S_n}\sgn(p)\prod_{i=1}^n(A)_{i,q^{-1}\left(p(i)\right)}=\\
 & =\sgn(q)\sum_{p\in S_n}\sgn(q)\sgn(p)\prod_{i=1}^n(A)_{i,q^{-1}\left(p(i)\right)}=\sgn(q)\sum_{p\in S_n}\sgn(h)\prod_{i=1}^n(A)_{i,h(i)}=\\
 & =\sgn(q)\det(A)
\end{align*}
\end{e}
\end{e}

\begin{e}{Dùsledek}{0}{0}
Má-li matice dva shodné sloupce nebo øádky, má automaticky nulovı determinant (pøehozením právì tìch dvou øádkù nebo sloupcù vzniknou shodné matice se stejnım determinantem, ale má se zmìnit znaménko).
\end{e}

\begin{e}{Vìta}{0}{Determinant jako lineární funkce}
Determinant matice $A$ je lineární funkcí kadého jejího øádku i kadého sloupce, tj. platí
\begin{penumerate}
    \item $$\det\left(\begin{matrix}
a_{1,1}& \dots & a_{1,n}\\
\vdots & & \vdots \\
b_{i,1}& \dots & b_{i,n}\\
\vdots & & \vdots \\
a_{n,1}& \dots & a_{n,n}\\
\end{matrix}\right)
+ \det\left(\begin{matrix}
a_{1,1}& \dots & a_{1,n}\\
\vdots & & \vdots \\
c_{i,1}& \dots & c_{i,n}\\
\vdots & & \vdots \\
a_{n,1}& \dots & a_{n,n}\\
\end{matrix}\right)
= \det\left(\begin{matrix}
a_{1,1}& \dots & a_{1,n}\\
\vdots & & \vdots \\
b_{i,1} + c_{i,1}& \dots & b_{i,n} + c_{i,n}\\
\vdots & & \vdots \\
a_{n,1}& \dots & a_{n,n}\\
\end{matrix}\right)$$
    \item $$\det\left(\begin{matrix}
a_{1,1}& \dots & a_{1,n}\\
\vdots & & \vdots \\
\kappa a_{i,n}& \dots & \kappa a_{i,n}\\
\vdots & & \vdots \\
a_{n,1}& \dots & a_{n,n}\\
\end{matrix}\right)
= \kappa \det(A)$$
\end{penumerate}

\begin{e}{Dùkaz}{0}{0}
První èást plyne z distributivity sèítání vzhledem k násobení -- kadı èlen sumy (produkt prvkù) obsahuje jeden prvek typu $b_{i,p(i)} + c_{i,p(i)}$ pro nìjakou permutaci a ten je moné rozepsat. Druhá èást se dokáe podobnì díky komutativitì násobení -- prvek $\kappa$ je také obsaen v kadém èlenu sumy právì jednou, take je ho moné \uv{vytknout}.
\end{e}
\end{e}

\begin{e}{Vìta}{0}{Determinant souèinu matic}
Nech $A$ a $B$ jsou ètvercové matice stejného øádu $n$ nad tìlesem $T$. Potom platí:
$$\det(A\cdot B)=\det(A)\cdot\det(B)$$

\begin{e}{Dùkaz}{0}{0}
Je-li jedna z matic singulární, je jejich souèin singulární a tedy má nulovı determinant; stejnì jako je nulovı souèin determinantù pùvodních matic. Jsou-li obì matice regulární, lze $A$ rozloit na nìjakı souèin $E_1\cdot E_2\cdot\dots\cdot E_k$ elementárních matic. Potom 
\begin{align*}
\det(AB) & = \det(E_1E_2\dots E_k B)=\det(E_1)\cdot\det(E_2\dots E_k B)\\
& =\det(E_1)\det(E_2)\dots\det(E_k)\det(B)=\det(A)\det(B)
\end{align*}
protoe víme, jakım zpùsobem elementární úpravy (ekvivalent elementárních matic ve vzorci) mìní determinant.
\end{e}
\end{e}

\begin{e}{Dùsledek}{0}{0}
Ètvercová matice je regulární, právì kdy má nenulovı determinant.
\end{e}

\subsection{Úpravy determinantù, vıpoèet}

\begin{obecne}{Gaussova eliminace}
Gaussova metoda spoèívá v provedení takovıch úprav matice, které nemìní hodnotu determinantu, ale zjednoduší vıpoèet jeho hodnoty. Cílem provádìnıch úprav je získat trojúhelníkovou matici \textbf{A} (kde pro $i > j$ je $a_{i,j} = 0$), nebo pro trojúhelníkové matice platí

$$\det A = a_{1,1} a_{2,2}  \dots  a_{n,n}$$

\noindent
tzn. determinant je roven souèinu prvkù hlavní diagonály matice.

\noindent
Pøi úpravách matice pro vıpoèet determinantu postupujeme podle tìchto pravidel:
\begin{pitemize}
\item Pokud \textbf{B} vznikne z \textbf{A} vımìnnou dvou øádku nebo sloupcù potom $\det B = -\det A$
\item Pokud \textbf{B} vznikne z \textbf{A} vynásobením øádku nebo sloupce skalárem c, potom $\det B = c.\det A$
\item Pokud \textbf{B} vznikne z \textbf{A} pøiètením násobku jednoho øádku k jinému, nebo pøidáním násobku sloupce k jinému sloupci potom $\det B = \det A$
\end{pitemize}

\noindent
Opakovanım pouitím uvedenıch pravidel pøevedeme matici na trojúhelníkovou a pro tu poté snadno spoèteme determinant.
\end{obecne}



\subsection{Geometrickı smysl determinantu}

\begin{obecne}{Matice øádu 2}
Absolutní hodnotu determinantu matice øádu 2
$$\det\left(
\begin{array}{cc}
a & b\\
c & d
\end{array}
\right) = ad - bc
$$
lze interpretovat jako obsah rovnobìníku s vrcholy v bodech $(0,0)$, $(a,c)$, $(b, d)$ a $(a+b, c+d)$. Znaménko determinantu urèuje vzájemnou orientaci vektorù $(a,c)$, $(b, d)$. $\det A$ je kladnı, pokud úhel mezi vektory $(a,c)$, $(b, d)$ mìøenı v kladném smìru (tedy proti smìru hodinovıch ruèièek) menší ne $\pi$, a zápornı, pokud je tento úhel vìtší ne $\pi$.
\end{obecne}

\begin{obecne}{Matice øádu 3}
Podobnı geometrickı vıznam jako pro matici øádu 2 najdeme i pro matice $B = (b_{i,j})$ øádu 3. Øádkové vektory

$$b_1 = (b_{1,1}, b_{1,2}, b_{1,3}), b_2 = (b_{2,1}, b_{2,2}, b_{2,3}), b_3 = (b_{3,1}, b_{3,2}, b_{3,3})$$

urèují v tøídimenzionálním prostoru rovnobìnostìn, jeho objem je roven $\left| \det B \right|$. Pokud je $\det B$ kladnı, tak je posloupnost vektorù $b_1, b_2, b_3$ pravotoèivá, a levotoèivá, pokud je $\det B$ zápornı.
\end{obecne}

\begin{obecne}{Matice vyšších øádù}
I v reálnıch prostorech vyšších øádù lze determinant chápat jako objem obecného \emph{n}-rozmìrného rovnobìnostìnu, pøípadnì jako pravotoèivost, respektive levotoèivost posloupnosti $b_1, b_2,  \dots , b_n$.
\end{obecne}

\begin{e}{Definice}{0}{Pravotoèivá a levotoèivá soustava prostorovıch kartézskıch souøadnic}
Pøedstavte si, e v místì, kde stojíte, je poèátek prostorové kartézské soustavy. Osa x nech smìøuje pøímo vpøed (smìrem, kterım se díváte), osa y nech smìøuje vlevo a osa z nech smìøuje vzhùru. Taková soustava se nazıvá \emph{pravotoèivá souøadná soustava}.

Zamìníme-li osy x a y, získáme \emph{souøadnou soustavu levotoèivou}. Obvykle se pracuje s pravotoèivou souøadnou soustavou.

Mnemotechnická pomùcka: Soustava souøadnic je pravotoèivá pokud pøi naznaèení kladného smìru osy z zdvienım palcem pravé ruky naznaèují ostatní prsty smìr od kladného smìru osy x ke kladnému smìru osy y.
\end{e}


\subsection{Minory a inversní matice}

\begin{e}{Definice}{0}{Minor}
Mìjme ètvercovou matici $A_{ij}$, kterou získáme z matice $A$ odstranìním i-tého øádku a j-tého sloupce. Determinant matice $A_{ij}$, tzn. $\det A_{ij}$ nazıváme \emph{subdeterminantem} (té \emph{minorem}) pøíslušnım k prvku $a_{i,j}$ matice $A$.
\end{e}

\begin{obecne}{Vıpoèet determinantu rozvojem podle øádkù (sloupcù)}
Algebraickı doplnìk lze pouít k vıpoètu determinantu \emph{n}-tého øádu. Pro libovolné (pevnì dané) \emph{i} lze determinant matice $A$ vyjádøit pomocí algebraickıch doplòkù jako

$$\det( A ) = \sum_{j=1}^n a_{i,j}\cdot (-1)^{i+j}\det( A_{ij} )$$

Tento postup je oznaèován jako \emph{rozvoj (rozklad) determinantu podle i-tého øádku}. Ekvivalentnì lze determinant vyjádøit \emph{rozvojem (rozkladem) podle j-tého sloupce}. Èíslo $(-1)^{i+j}\det( A_{ij} )$ se nìkdy nazıvá \emph{kofaktorem} nebo \emph{algebraickım doplòkem}.
\end{obecne}

\begin{e}{Definice}{0}{Adjungovaná matice}
Pro ètvercovou matici $A$ definujeme \emph{adjungovanou matici} $\adj A$ pøedpisem
$$(\adj A)_{i,j}=(-1)^{i+j}\cdot\det(A_{ji})$$
kde $A_{ji}$ jsou minory matice $A$ (s vynechanım $j$-tım øádkem a $i$-tım sloupcem -- pozor na obrácené poøadí indexù!). Prvky adjungované matice jsou vlastnì algebraické doplòky v transponované matici $A^T$.
\end{e}

\begin{e}{Definice}{0}{Inversní matice}
Pro ètvercovou matici $A$ øádu $n$ definujeme inverzní matici $A^{-1}$ pøedpisem
$$A.A^{-1} = A^{-1}.A = I_n$$
kde $I_n$ je jednotková matice. Inversní matici lze sestrojit pouze pro regulární matici.
\end{e}

\begin{e}{Vìta}{0}{Vıpoèet inversní matice podle minorù}
Pro kadou regulární matici $A$ nad tìlesem $T$ platí: 
$$A^{-1}=\frac{1}{\det A}\cdot(\adj A)$$
$$A^{-1}_{i,j}=(-1)^{i+j}\frac{\det(A_{ji})}{\det A}$$

\begin{e}{Dùkaz}{0}{0}
Z maticového souèinu $A\cdot\adj A$:
\begin{penumerate}
    \item $i$-tı øádek $A$ $\times$ $i$-tı sloupec $\adj A$ (obs. determinanty minorù odp. $i$-tému øádku) dá dohromady $\det A$
    (z rozvoje determinantu podle øádku)
    \item $j$-tı øádek $A$ $\times$ $i$-tı sloupec $\adj A$ dá dohromady $0$, protoe jde o stejnı princip pro matici, kde $i$-tı
    øádek je nahrazen $j$-tım (2 stejné øádky)
\end{penumerate}
Potom $A\cdot\adj A = \det A\cdot I_n$ a to u dává $\frac{1}{\det A}\adj A = A^{-1}$.
\end{e}
\end{e}


\subsection{Cramerovo pravidlo}
Cramerovo pravidlo je metoda umoòující nalezení øešení soustavy lineárních algebraickıch rovnic.

\begin{obecne}{Postup}
Mìjme soustavu lineárních rovnic, která obsahuje stejnı poèet neznámıch jako je poèet rovnic. Oznaème matici soustavy $A$. Dále oznaème $A_i\;$ jako matici, kterou získáme z matice $A$, nahradíme-li v ní $i$-tı sloupec sloupcem pravıch stran soustavy rovnic.

Pokud zapíšeme matice soustavy a vektor pravıch stran jako

$$
A =
\left( \begin{array}{cccc}
    a_{11} & a_{12} & \dots  & a_{1n} \\
	a_{21} & a_{22} &  \dots  & a_{2n} \\
	\vdots  &  \vdots  &  \ddots  &  \vdots \\
	a_{m1} & a_{m2} &  \dots  & a_{mn}
\end{array} \right), 
B = \left( \begin{array}{cccc} b_1 \\b_2 \\ \vdots  \\b_m \end{array} \right)
$$

pak má tvar

$$
A_i=
\left( \begin{array}{cccccccc}
	a_{11} & a_{12} & \dots & a_{1,i-1} & b_1 & a_{1,i+1} & \dots  & a_{1n} \\
	a_{21} & a_{22} & \dots & a_{2,i-1} & b_2 & a_{2,i+1} & \dots  & a_{2n} \\
	\vdots & \vdots & \vdots & \vdots & \vdots & \vdots & \ddots & \vdots \\
	a_{m1} & a_{m2} & \dots & a_{m,i-1} & b_m & a_{m,i+1} & \dots & a_{mn}
\end{array} \right)
$$

Pokud je determinant matice soustavy nenulovı, $\det A \neq 0$, tzn. matice je regulární, pak má soustava právì jedno øešení, pro které platí

$$x_i = \frac{\det A_i}{\det A}$$

pro $i = 1,2, \dots ,n$.
\end{obecne}

\begin{e}{Dùkaz}{0}{0}
Pro soustavu $Ax=b$ -- rozepíšeme $x=A^{-1}b$, ze vzorce pro inversní matici plyne
$$x=\frac{1}{\det A}(\adj A)\cdot b$$
take pro $x_i$ vychází 
$$x_i=\frac{1}{\det A}((\adj A)b)_i=\frac{1}{\det A}\cdot\sum_{j=1}^n(\adj A)_{i,j}\cdot b_j=\frac{1}{\det A}\cdot\det A_i$$
\end{e}

\begin{e}{Pøíklad}{0}{0}
Úkolem je øešit soustavu rovnic
 $$x + y = 3$$
 $$x - 2y = 1$$

Determinant matice soustavy je
 $$\det A = \left| \begin{array}{cc}1 & 1 \\ 1 & -2\end{array} \right| = -3$$

Ponìvad je $\det A \neq 0$, lze pouít Cramerovo pravidlo.

Dále urèíme
 $$\det A_1 = \left| \begin{array}{cc}3 & 1 \\ 1 & -2\end{array} \right| = -7$$
 $$\det A_2 = \left| \begin{array}{cc}1 & 3 \\ 1 & 1\end{array} \right| = -2$$

Øešení má tedy tvar
 $$x = \frac{\det A_1}{\det A} = \frac{-7}{-3} = \frac{7}{3}$$
 $$y = \frac{\det A_2}{\det A} = \frac{-2}{-3} = \frac{2}{3}$$

Zkouškou se pøesvìdèíme, e se skuteènì jedná o øešení uvedené soustavy.
\end{e}
