\subsection{Sbìrnice, protokoly}

\begin{pitemize}
	\item \textbf{Struktura sbìrnice}: datové linky, adresové linky, øídící linky
	\item \textbf{Synchronní pøenos} (vznik události je dán hodinovým signálem) vs. \textbf{asynchronní pøenos} (vznik události je urèen pøedcházející událostí~-- napr. signalizáciou zaèiatku dát) 
	\item \textbf{Parametry sbìrnice}: 
	\begin{pitemize}
	  \item \emph{datová šíøka}~-- poèet pøenášených bitù v jednom okamžiku,
	  \item \emph{kapacita}~-- poèet bitù pøenesených za èas,
	  \item \emph{rychlost}~-- kapacita sbìrnice normovaná k jednotce informace. 
	\end{pitemize}  
	\item \textbf{Øízení požadavkù}: 
	\begin{pitemize}
	  \item \emph{centrální}~-- náhodné, dle poøadí vzniku požadavkù, prioritní,
	  \item \emph{distribuované}~-- kolizní (CSMA/CD), token bus, prioritní linka (daisy chain).
	\end{pitemize} 
	\item \textbf{Pøenos dat po sbìrnici} mùže probíhat buï za úèasti procesoru (zdroj $\rightarrow$ CPU $\rightarrow$ cíl), nebo bez. Bez procesoru to mùže být napø.:
	\begin{pitemize}
		\item dávkový režim~-- domluva mezi CPU a øadièem na dobì obsazení sbìrnice (napr. pomocou zdvihnutia \uv{lock flagu} na zbernici)
		\item kradení cyklù~-- øadiè na dobu pøenosu \uv{uspí} procesor (nelze uspat na dlouho, je to technicky nároènìjší)
		\item transparentní režim~-- øadiè rozezná, kdy procesor nepoužívá sbìrnici, obvykle nelze vìtší pøenosy najednou
		\item DMA (Direct Memory Access)~-- speciální jednotka pro provádìní pøenosù dat (mezi zaøízeními a pamìtí)
	\end{pitemize}
	Jednou z technik, používaných k pøenosu dat po sbìrnici øadièi DMA, je \emph{scatter-gather}. Znamená to, že v rámci jednoho pøenosu se zpracovává víc ne nutnì souvislých blokù dat. 
	\begin{pitemize}
	    \item \emph{scatter}~-- DMA øadiè v rámci 1 pøenosu uloží z 1 místa data na nìkolik rùzných míst (napø hlavièky TCP/IP - jinak zbyteèné kopírování)  
	    \item \emph{gather}~-- napø. pøi stránkování pamìti - naèítání stránek, které fyzicky na disku nemusí být u sebe, složení na 1 místo do pamìti.
	\end{pitemize}
\end{pitemize}

Pøíklady sbìrnic:
\begin{pitemize}
	\item ISA, EISA
	\item ATA, ATAPI~-- UltraDMA, Serial-ATA (SATA)
	\item SCSI (Small Computer System Interface)
	\item PCI, PCI-X, PCI Express
	\item AGP (Advanced Graphics Port)
	\item USB (Universal Serial Bus)
	\item FireWire (IEEE 1394)
	\item RS485
	\item $I^{2}C$
\end{pitemize}

Pøíbuzné sbìrnic:
\begin{pitemize}
	\item IrDA
	\item Bluetooth
	\item Wi-Fi, WiMAX 
\end{pitemize}
