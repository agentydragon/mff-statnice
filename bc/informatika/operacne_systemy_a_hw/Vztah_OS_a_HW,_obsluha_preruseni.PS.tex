\subsection{Vztah OS a HW, obsluha pøerušení}

\begin{obecne}{Zjištìní zmìny stavu I/O zaøízení:}
\begin{pitemize}
	\item \emph{asynchronní pøerušení}~-- zašle zaøízení
	\item \emph{polling}~-- peridická kontrola stavu zaøízení
\end{pitemize}
\end{obecne}

\begin{obecne}{Druhy pøerušení:}
\begin{pitemize}
	\item \emph{synchronní}~-- zámìrnì (instrukce TRAP~-- vstup do OS), výjimky (nesprávné chování procesu)~-- zpracuje se okamžitì
	\item \emph{asynchronní}~-- vnìjší událost (napø. pøíchod dat)~-- zpracuje se po dokonèení aktuální instrukce 
\end{pitemize}
\end{obecne}

\begin{obecne}{Obsluha pøerušení:}
\begin{pitemize}
	\item OS se ujme øízení
	\item uloží se stav CPU (obsah registrù, èítaè, ...)
	\item analyzuje se pøerušení, vyvolá se pøíslušná obsluha (pokud není pøerušení blokováno)
	\item obslouží se pøerušení (napø. se zavolá obslužná procedura)
	\item obnoví se stav CPU a aplikace pokraèuje, popø. mùže dojít k pøeplánování 
\end{pitemize}
\end{obecne}

\begin{obecne}{I/O software (vrstvy):}
\begin{pitemize}
	\item uživatelský I/O software
	\item I/O nezávislý subsystém
	\item ovladaèe zaøízení
	\item obsluha pøerušení 
\end{pitemize}
\end{obecne}

\begin{obecne}{Cíle I/O software:}
\begin{pitemize}
	\item nezávislost zaøízení~-- programy nemusí vìdìt, s jakým pøesnì pracují
	\item jednotné pojmenování (/dev)
	\item pøipojení (mount)~-- vymìnitelná zaøízení
	\item obsluha chyb
\end{pitemize}
\end{obecne}
