\subsection{Èasová složitost algoritmù, složitost v nejhorším a prùmìrném pøípadì}


\begin{e}{Definice}{0}{èasová složitost}
  \textbf{Èasovou složitostí} algoritmu rozumíme závislost jeho èasových nárokù na
  velikosti konkrétních vstupních dat. Analogicky se definuje i \textbf{pamìová
  složitost}. Dobu zpracování úlohy o velikosti $n$ znaèíme $T(n)$\\
  Èasovou složitost èasto zkoumáme z nìkolik hledisek:
  \begin{pitemize}
    \item \textbf{v nejhorším pøípadì}~-- maximální poèet operací pro nìjaká data, 
    \item \textbf{v nejlepším pøípadì}~-- minimální poèet operací pro nìjaká data,
    \item \textbf{v prùmìrném (oèekávaném) pøípadì}~-- prùmìr pro všechna možná
  vstupní data (nìkdy též støední hodnota náhodné velièiny $T(n)$).
  \end{pitemize}

  \begin{e}{Poznámka}{0}{0}
  Jako jednu \uv{operaci}, nebo-li \emph{krok algoritmu} rozumíme jednu elementární 
  operaci nìjakého abstraktního stroje (napø. Turingova stroje), proveditelnou v 
  konstantním èase. Intuitivnì
  je možné chápat to jako nìkolik operací poèítaèe, které dohromady netrvají více,
  než nìjakou pevnì danou dobu.
  \end{e}
\end{e}

\begin{e}{Poznámka}{0}{0}
  Èasová složitost problému je rovna složitosti nejlepšího algoritmu øešícího
  daný problém.
\end{e}


\subsubsection*{Asymptotická složitost}

\begin{e}{Definice}{0}{0}
  Øekneme, že funkce $f(n)$ je \textbf{asymptoticky menší nebo rovna} než $g(n)$,
  znaèíme $f(n)$ je $O(g(n))$, právì tehdy, když
  $$ \exists c>0\ \exists n_0\ \forall n>n_0: 0 \leq f(n) \leq c \cdot g(n)$$
  Funkce $f(n)$ je \textbf{asymptoticky vìtší nebo rovna} než $g(n)$, znaèíme
  $f(n)$ je $\Omega(g(n))$, právì tehdy, když
  $$ \exists c>0\ \exists n_0\ \forall n>n_0: 0 \leq c \cdot g(n) \leq f(n)$$
  Funkce $f(n)$ je \textbf{asymptoticky stejná} jako $g(n)$, znaèíme $f(n)$ je
  $\Theta(g(n))$, právì tehdy, když
  $$ \exists c_1,c_2>0\ \exists n_0\ \forall n>n_0: 0 \leq c_1 \cdot g(n) \leq
  f(n) \leq c_2 \cdot g(n)$$
\end{e}

\begin{e} {Poznámka}{0}{0}
  Asymptotická složitost zkoumá chování algoritmù na \uv{velkých} datech a dle
  jejich složitosti je zaøazuje do skupin (polynomiální, exponenciální\dots).
  Pøi zkoumání se zanedbávají aditivní a multiplikativní konstanty. 
\end{e}

\subsubsection*{Amortizovaná složitost}

\begin{e}{Definice}{0}{Amortizovaná složitost}
  \emph{Amortizovaná èasová složitost} poèítá prùmìrný èas na jednu operaci pøi provedení
  posloupnosti operací. Používá se typicky pro poèítání èasové složitosti operací
  nad datovými strukturami. Dává realistiètìjší horní odhad složitosti
  posloupnosti operací, než poèítání s nejhorším pøípadem u každé operace.
\end{e}

\begin{obecne}{Agregaèní metoda}
  Spoèítáme (nejhorší možný) èas $T(n)$ pro posloupnost operací. Amortizovaná cena
  jedné operace je potom $\frac{T(n)}{n}$.
\end{obecne}

\begin{obecne}{Úèetní metoda}
  Od každé operace \uv{vybereme} urèitý \uv{obnos}, ze kterého \uv{zaplatíme} za
  danou operaci a pokud nìco zbude, dáme to na úèet. Pokud je operace dražší než
  kolik je její obnos, tak potøebný rozdíl vybereme z úètu. Zùstatek na úètu
  musí být stále nezáporný. Pokud uspìjeme, tak \uv{obnos} = amortizovaná cena
  jedné operace.
\end{obecne}

\begin{e}{Poznámka}{0}{0}
  Jde o to, že nìkterá operace mùže trvat krátce, ale \uv{rozháže} datovou
  strukturu, takže následující operace potøebují víc èasu. Nebo naopak trvá
  dlouho a datovou strukturu \uv{uspoøádá}, takže ostatní operace jsou kratší.
\end{e}
\begin{e}{Report}{0}{Skopal} Casova zlozitost
\end{e}
