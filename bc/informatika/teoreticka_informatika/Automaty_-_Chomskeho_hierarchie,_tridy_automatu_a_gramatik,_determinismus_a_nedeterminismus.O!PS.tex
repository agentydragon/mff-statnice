\def\implies{\Rightarrow}
\def\Nat{\mathbb{N}}
\def\Real{\mathbb{R}}
\def\isimplied{\Leftarrow}
\def\onlyif{\Leftrightarrow}
\def\c#1{\mathcal{#1}}


\subsection{Automaty -- Chomského hierarchie, tøídy automatù a gramatik, determinismus a nedeterminismus.}
\begin{pitemize}
\item Popište jednotlivé tøídy jazykù a jejich vztahy; definujte tøídy pomocí odpovídajících gramatik. Napište priklady gramatik pro jednotlivé tøídy.

\item Popište automaty, ktere tyto tridv jazyku rozpoznávají i  s ohledem na jejich (ne)deterministicnost.
\end{pitemize}

\subsubsection*{Tøídy automatù a gramatik}

\begin{definiceN}{Koneèný automat}
\emph{Koneèný automat} je pìtice $A = (Q,X,\delta,q_0,F)$, kde $Q$ je stavový prostor (množina všech možných stavù), $X$ je \emph{abeceda} (množina symbolù), $\delta$ je pøechodová funkce $\delta: Q\times X\to Q$, $q_0\in Q$ je poè. stav a $F\subseteq Q$ množina koncových stavù.
\end{definiceN}

\begin{definice}
\emph{Slovo} $w$ je posloupnost symbolù v abecedì $X$. \emph{Jazyk} $L$ je množina slov, tedy $L\subseteq X^{\ast}$, kde $X^{\ast}$ je množina všech posloupností symbolù abecedy $X$. $\mathbf{\lambda}$ je prázdná posloupnost symbolù. \emph{Rozšíøená pøechodová funkce} je $\delta^{\ast}:Q\times X^{\ast} \to Q$ - tranzitivní uzávìr $\delta$. Jazyk rozpoznávaný koneèným automatem -- \emph{regularní jazyk} je $L(A) = \{ w | w\in X^{\ast}, \delta^{\ast}(q_0,w)\in F \}$. \emph{Pravá kongruence} je taková relace ekvivalence na $X^{\ast}$, že $\forall u,v,w\in X^{\ast}: u \sim v \implies uw \sim vw$.\footnote{Pokud dvì rùzná slova $u,v$ pøevedou automat do stejného stavu (=jsou navzájem ekvivalentní ($u \sim v$)), pak musí patøit do stejné tøídy rozkladu. Pokud k tìmto dvìma slovùm pøidáme stejné slovo zprava, pak tato zøetìzená slova budou opìt patøit do stejné tøídy rozkladu (=musí být navzájem ekvivalentní ($uw \sim vw$)). A toto je právì ta vlastnost definující pravou kongruenci.} Je \emph{koneèného indexu}, jestliže $X^{\ast}/\sim$ (rozklad na tøídy ekvivalence) má koneèný poèet tøíd. \begin{footnotesize}\end{footnotesize} 
\end{definice}

\begin{vetaN}{Nerodova}
Jazyk $L$ nad koneènou abecedou $X$ je rozpoznatelný kon. automatem $\Leftrightarrow$ existuje pravá kongruence koneèného indexu $\sim$ na $X^{\ast}$ tak, že $L$ je sjednocením jistých tøíd rozkladu $X^{\ast}/\sim$.\footnote{Dùležité tedy je, že pokud je jazyk regulární, pak pro nìj musí existovat pravá kongruence, která (což je nejdùležitìjší) rozkládá všechna slova jazyka do koneènì mnoha tøíd.}
\end{vetaN}
\begin{vetaN}{Pumping (iteraèní) lemma}
Pro jazyk rozpoznatelný kon. automatem (tzn. regulární) $L$ existuje $n\in \Nat$ tak, že libovolné slovo $z\in L,|z|\geq n$ lze psát jako $uvw$, kde $|uv|\leq n$, $|v|\geq 1$ a $\forall i\geq 0: uv^{i}w\in L$.\footnote{Platí i pro koneèné jazyky: když je jazyk koneèný, tak si za $n$ staèí vzít délku nejdelšího slova a pak to pro všechny slova delší než n (tj. žádná) platí taky. }
\end{vetaN}

\begin{definice}
Dva automaty jsou \emph{ekvivalentní}, jestliže pøijímají stejný jazyk. \emph{Homomorfismus (isomorfismus)} automatù je zobrazení, zachovávající poè. stav, pøech. funkci i konc. stavy (+ prosté a na). Pokud existuje homomorfismus automatù $A\to B$, pak jsou tyto dva ekvivalentní (jen 1 implikace!). \emph{Dosažitelný stav} $q$ - $\exists w \in X^{\ast}: \delta^{\ast}(q_0,w) = q$. Relace ekvivalence je \emph{automatovou kongruencí}, pokud zachovává konc. stavy a pøech. funkci. Ke každému automatu existuje \emph{redukt} - ekvivaletní automat bez nedosažitelných a navzájem ekvivalentních stavù. Ten je urèen jednoznaènì pro daný jazyk (až na isomorfismus), proto lze zavést normovaný tvar.
\end{definice}

\begin{poznamkaN}{Operace s jazyky}
S jazyky lze provádìt množinové operace ($\cup, \cap$), rozdíl ($\{ w | w \in L_1 \& w \notin L_2 \}$), doplnìk ($\{ w | w \notin L \}$), dále zøetìzení ($L_1\cdot L_2 = \{ uv | u \in L_1, v \in L_2 \}$), mocniny ($L^0={\lambda}, L^{i+1} = L^{i}\cdot L$), iterace ($L^{\ast}=L^0 \cup L^1 \cup L^2 \cup ...$), otoèení ($L^R = \{ u^R | u\in L \}$), levý ($L_2\setminus L_1 = \{ v | uv \in L_1, u \in L_2 \}$) i pravý
($L_1 / L_2 = \{ u | uv \in L_1, v \in L_2 \}$) kvocient $L_1$ podle $L_2$  a derivace (kvocienty podle jednoslovného jazyka). Tøída jazykù rozpoznatelných koneènými automaty je na tyto operace uzavøená.
\end{poznamkaN}

\begin{definiceN}{Regulární jazyky}
\emph{Tøída reguláních jazykù} nad abecedou $X$ je nejmenší tøída, která obsahuje $\emptyset$, $\forall x \in X$ obsahuje ${x}$ a je uzavøená na sjednocení, iteraci a zøetìzení.
\end{definiceN}

\begin{vetaN}{Kleenova}
Jazyk je regulární $\Leftrightarrow$ je rozpoznatelný koneèným automatem.\footnote{Dùkaz se dá indukcí podle poètu hran v nedeterministickém automatu.}
\end{vetaN}

\begin{definiceN}{Regulární výrazy}
\emph{Regulární výrazy} nad abecedou $X={x_1,...,x_n}$ jsou nejmenší množina slov v abecedì ${x_1,...,x_n,\emptyset,\lambda,^{+},\cdot,^{\ast},(,)}$, která obsahuje výrazy $\emptyset$ a $\lambda$ a $\forall i$ obsahuje $x_i$ a je uzavøená na sjednocení ($+$), zøetìzení ($\cdot$) a iterace ($^{\ast}$). \emph{Hodnota reg. výrazu} $a$ je reg. jazyk $[a]$, lze takto reprezentovat každý reg. jazyk.
\end{definiceN}

\begin{definiceN}{Dvoucestné koneèné automaty}
\emph{Dvoucestný koneèný automat} je pìtice $(Q,X,\delta,q_0,F)$, kde oproti kon. automatu je $\delta:Q\times X\to Q\times \{-1,0,1\}$ (tj. pohyb ètecí hlavy). Pøijímá slovo, pokud výpoèet zaèal vlevo v poè. stavu a ètecí hlava opustila slovo $w$ vpravo v konc. stavu (mimo slovo konèí výpoèet okamžitì).
\end{definiceN}
\begin{poznamka}
Jazyky pøijímané dvoucestnými automaty jsou regulární - každý dvoucestný automat lze pøevést na (nedeterministický) koneèný automat.
\end{poznamka}


\begin{definiceN}{Zásobníkové automaty}
\emph{Zásobníkový automat} je sedmice $M=(Q,X,Y,\delta,q_0,Z_0,F)$, kde proti koneèným automatùm je $Y$ abeceda pro symboly na zásobníku, $Z_0$ poèáteèní symbol na zásobníku a funkce instrukcí $\delta:Q\times(X\cup\{\lambda\})\times Y\ \to\ \c{P}(Q\times Y^{\ast})$. Je z principu nedeterministický; vždy se nahrazuje vrchol zásobníku, neète ale pokaždé vstupní symboly. Instrukci $(p,a,Z)\to(q,w)$ lze vykonat, pokud je automat ve stavu $p$, na zásobníku je $Z$ a na vstupu $a$. Vykonání instrukce znamená zmìnu stavu, pokud $a\neq\lambda$, tak i posun ètecí hlavy a odebrání $Z$ ze zásobníku, kam se vloží $w$ (prvním písmenem nahoru). Výpoèet konèí buï pøeètením slova, nebo v pøípadì, že pro danou situaci není definována instrukce
(\emph{Situace} zás. automatu je trojice $(p,u,v)$, kde $p\in Q$, $u$ je nepøeètený zbytek slova a $v$ celý zásobník ).

Pøijímat slovo je možné buï koncovým stavem (slovo je pøeèteno a automat v konc. stavu), nebo zásobníkem (slovo je pøeèteno a zásobník prázdný -- konc. stavy jsou v takovém pøípadì nezajímavé - $F=\emptyset$).
\end{definiceN}

\begin{poznamka}
Pro zás. automat pøijímající konc. stavem vždy existuje ekvivalentní automat ($L(A_1)=L(A_2)$) pøijímající zásobníkem a naopak.
\end{poznamka}

\begin{definiceN}{Pøepisovací systém}
\emph{Pøepisovací (produkèní) systém} je dvojice $R=(V,P)$, kde $V$ je koneèná abeceda a $P$ množina pøepisovacích pravidel (uspoøádaných dvojic prvkù z $V^{\ast}$). Slovo $w$ se \emph{pøímo pøepíše} na $z$ ($w\Rightarrow z$), pokud $\exists u,v,x,y\in V^{\ast}: w = xuy, z = xvy, (u,v)\in P$. Derivace (\emph{odvození}) je zøetìzení nìkolika pøímých pøepsání.
\end{definiceN}

\begin{definiceN}{Formální (generativní) gramatika}
\emph{Formální gramatika} je ètveøice $G=(V_N,V_T,S,P)$, kde $V_N$ je množina neterminálních symbolù (ostatní znaky napø. $S$), $V_T$ množina terminálních symbolù
("znaky z abecedy"), $S$ startovací symbol ($S\in V_N$) a $P$ množina pravidel. \emph{Jazyk generovaný gramatikou} je $L(G)=\{w | w\in V_T^{\ast}, S\Rightarrow^{\ast}w\}$.
\end{definiceN}
\begin{veta}
Každý bezkontextový jazyk je rozpoznáván zásobníkovým automatem, pøijímajícím prázdným zásobníkem. Stejnì pro každý zásobníkový automat existuje bezkontextová gramatika, která generuje jazyk jím pøijímaný.
\end{veta}

\begin{poznamkaN}{Vlastnosti bezkontextových gramatik}
Bezkontextová gramatika je \emph{redukovaná}, pokud $\forall X\in V_N$ existuje terminální slovo $w\in V_T^{\ast}$ tak, že $X\Rightarrow^{\ast} w$ a navíc $\forall X\in V_N, X\neq S$ existují slova $u,v$ tak, že $S\Rightarrow^{\ast}uXv$. Ke každé bezkontextové gramatice lze sestrojit ekvivalentní redukovanou.

Pro každé terminální slovo v bezkontextové gramatice existují derivace, které se liší jen poøadím použití pravidel (a prohozením nìkterých pravidel dostanu stejné terminální slovo), proto lze zavést \emph{levé (pravé) derivace} - tj. \emph{kanonické} derivace. Pokud $X\Rightarrow^{\ast}w$, pak existuje i levá (pravá) derivace. Znázornìní prùbìhu derivací je možné urèit \emph{derivaèním stromem} -- urèuje jednoznaènì pravou/levou derivaci.

Bezkontextová gramatika je \emph{víceznaèná} (nejednoznaèná), pokud v ní existuje slovo, které má dvì rùzné levé derivace; jinak je \emph{jednoznaèná}. Jazyk je jednoznaèný, pokud k nìmu existuje generující jednoznaèná gramatika. Pokud je každá gramatika nìjakého jazyka nejednoznaèná, je tento \emph{podstatnì nejednoznaèný}. 
\end{poznamkaN}


\begin{definiceN}{Greibachové normální forma}
Gramatika je v \emph{Greibachové normální formì}, jsou-li všechna její pravidla ve tvaru $A\to au$, kde $a\in V_T$ a $u\in V_N^{\ast}$. Ke každému bezkontextovému jazyku existuje gramatika v G. normální formì tak, že $L(G)=L\setminus\{\lambda\}$. Každou bezkontextovou gramatiku lze pøevést do G. normální formy.
\end{definiceN}

\begin{poznamkaN}{Úpravy bezkontextových gramatik}
Spojením více pravidel ($A\to uBv, B\to w_1, ...B\to w_k$ se pøevede na $A\to uw_{1}v | ... | uw_{k}v$) dostanu ekvivalentní gramatiku. Stejnì tak odstranìním levé rekurze (pøevod pøes nový neterminál). 
\end{poznamkaN}

\begin{definiceN}{Chomského normální forma}
Pro gramatiku v \emph{Chomského normální formì} jsou všechna pravidla tvaru $X\to YZ$ nebo $X\to a$, kde $X,Y,Z\in V_N$, $a\in V_T$. Ke každému bezkontextovému jazyku $L$ existuje gramatika $G$ v Chomského normální formì tak, že $L(G)=L\setminus\{\lambda\}$
\end{definiceN}

\begin{poznamkaN}{Vlastnosti tøídy bezkontextových jazykù}
Tøída bezkontextových jazykù je uzavøená na sjednocení, zrcadlení, øetìzení, iteraci a pozitivní iteraci, substituci a homomorfismus, inverzní homomorfismus a kvocient s regulárním jazykem. Není uzavøená na prùnik a doplnìk.
\end{poznamkaN}

\begin{definiceN}{Dyckùv jazyk}
\emph{Dyckùv jazyk} je definován nad abecedou ${a_1,a'_1,...a_n,a'_n}$ gramatikou $$S\to \lambda | SS | a_{1}Sa'_1 | ... | a_{n}Sa'_n$$ Je bezkontextový, popisuje správná uzávorkování a lze jím popisovat výpoèty zásobníkových automatù, tedy i bezkontextové jazyky.
\end{definiceN}

\begin{definiceN}{Turingùv stroj}
\emph{Turingùv stroj} je pìtice $T=(Q,X,\delta,q_0,F)$, kde $X$ je abeceda, obsahující symbol $\varepsilon$ pro prázdné políèko, pøechodová funkce $\delta:(Q\setminus F)\times X\to Q\times X\times\{-1,0,1\}$ popisuje zmìnu stavu, zápis na pásku a posun hlavy. Výpoèet konèí, není-li definována žádná instrukce (spec. platí pro $q\in F$). \emph{Konfigurace} Turingova stroje jsou údaje, popisující stav výpoètu -- nejmenší souvislá èást pásky, obsahující všechny neprázdné buòky a ètenou buòku, vnitøní stav a poloha hlavy. \emph{Krok výpoètu} je $uqv \vdash wpz$ pro $u$ èást slova vlevo od akt. pozice na pásce, $v$ od èteného písmena dál a $q$ stav stroje. \emph{Výpoèet} je posloupnost krokù, slovo $w$ je pøijímáno, pokud $q_{0}w \vdash^{\ast} upv$, $p\in F$. Jazyky (množiny slov bez $\varepsilon$) pøijímané Turingovými stroji jsou \emph{rekurzivnì spoèetné}.
\end{definiceN}

\begin{veta}
Každý jazyk typu 0 (s gramatikou s obecnými pravidly) je rekurzivnì spoèetný.
\end{veta}

\subsubsection*{Chomského hierarchie}
\begin{definiceN}{Chomského hierarchie}
\emph{Chomského hierarchie} je rozdìlení gramatik do 4 tøíd podle omezení na pravidla:
\begin{figure}[!ht]
  \begin{center}
    \includegraphics[scale=1.7]{informatika/teoreticka_informatika/obrazky/chomski.png}
  \end{center}
\end{figure}
\end{definiceN}

\begin{poznamka}
S $\c{L}1 \supset \c{L}2$ nastává problém, protože bezkontextové gramatiky umožòují pravidla tvaru $X\to\lambda$. Øešením je pøevod na \emph{nevypouštìjící bezkontextové gramatiky} - takové bezkontextové gramatiky, které nemají pravidla typu $X\to\lambda$.
\end{poznamka}

\begin{vetaN}{o nevypouštìjících bezkontextových gramatikách}
Ke každé bezkontextové $G$ existuje nevypouštìjící bezkontextová $G_0$ tak, že $$L(G_0)=L(G)\setminus\{\lambda\}$$ Je-li $\lambda\in L(G)$, pak $\exists G_1$, t.ž. $L(G_1)=L(G)$ a jediné pravidlo v $G_1$ s $\lambda$ na pravé stranì je $S\to\lambda$ a $S$ není v $G_1$ na pravé stranì žádného pravidla.
\end{vetaN}

\begin{poznamkaN}{Lineární gramatiky}
Pro každou gramatiku typu G3 lze sestrojit koneèný automat, který pøijímá právì jazyk jí generovaný, stejnì tak pro každý koneèný automat lze sestrojit gramatiku G3. Levé lineární gramatiky také generují regulární jazyky, díky uzavøenosti na reverzi. \emph{Lineární gramatiky}, s pravidly typu $X\to uYv,X\to w$, kde $X,Y\in V_N, u,v,w\in V_T^{\ast}$, generují \emph{lineární jazyky} - silnìjší než regulární jazyky.
\end{poznamkaN}

\begin{definiceN}{Separovaná a nevypouštìjící gramatika}
\emph{Separovaná gramatika} je gramatika (obecnì libovolné tøídy), obsahující pouze pravidla tvaru $\alpha\to\beta$, kde buï $\alpha,\beta\in V_N^+$, nebo $\alpha\in V_N$ a $\beta\in V_T\cup\{\lambda\}$. \emph{Nevypouštìjící (monotónní) gramatika} (také se neomezuje na konkrétní tøídu) je taková, že pro každé pravidlo $u\to v$ platí $|u|\leq|v|$.
\end{definiceN}

\begin{poznamkaN}{Kontextové gramatiky}
Ke každé kontextové gramatice lze sestrojit ekvivalentní separovanou. Ke každé monotónní gramatice lze nalézt ekvivalentní kontextovou.
\end{poznamkaN}


\subsubsection*{Determinismus a nedeterminismus}

\begin{definiceN}{Nedeterministický koneèný automat}
\emph{Nedeterministický koneèný automat} je pìtice $(Q,X,\delta,S,F)$, kde $Q$ je mn. stavù, $X$ abeceda, $F$ mn. konc. stavù, $S$ množina poèáteèních stavù a $\delta:Q\times X\to \c{P}(Q)$ je pøechodová funkce. Slovo $w$ je takovým automatem pøijímáno, pokud existuje posloupnost stavù $\{q_i\}_{i=1}^n$ tak, že $q_1\in S$, $q_{i+1}\in\delta(q_i,x_i)$, $q_{n+1}\in F$.
\end{definiceN}

\begin{poznamka}
Pro každý nedeterministický koneèný automat $A$ lze sestrojit deterministický kon. automat $B$ tak, že jimi pøijímané jazyky jsou ekvivalentní (mùže to znamenat exponenciální nárùst poètu stavù).
\end{poznamka}


\begin{definiceN}{Deterministický zásobníkový automat}
\emph{Deterministický zásobníkový automat} je $M=(Q,X,Y,\delta,q_0,Z_0,F)$ takové, že $\forall p\in Q, \forall a\in (X\cup\{\lambda\}), \forall Z\in Y$ platí $|\delta(p,a,Z)|\leq 1\footnote{definuje ze v kazdem kroku si nemuzeme vybirat}$ a navíc pokud pro nìjaké $p, Z$ je $\delta(p,\lambda,Z)\neq\emptyset$, pak $\forall a\in X$ je $\delta(p,a,Z)=\emptyset\footnote{definuje ukonèení vypoctu}$. 
\end{definiceN}

\begin{poznamka}
Deterministický zásobníkový automat je \uv{slabší} než nedeterministický, rozpoznává \emph{deterministické bezkontextové jazyky} koncovým stavem a \emph{bezprefixové bezkontextové jazyky} prázdným zásobníkem (takové jazyky, kde $u\in L(M)\implies \forall w\in X^{\ast}: uw\notin L(M)$) - když se poprvé zásobník automatu vyprázdní, výpoèet urèitì konèí. 

Bezprefixové bezkontextové jazyky jsou vždy deterministické, opaènì to neplatí. Deterministický bezkontextový jazyk lze na bezprefixový pøevést zøetìzením s dalším symbolem, který není v pùvodní abecedì. 

Regulární jazyky a bezprefixové bezkontextové jazyky jsou neporovnatelné množiny.
\end{poznamka}

\begin{definiceN}{Nedeterministický Turingùv stroj}
\emph{Nedet. Turingùv stroj} je pìtice $T=(Q,X,\delta,q_0,F)$, kde oproti deterministickým je $\delta:(Q\setminus F)\times X \to \c{P}(Q\times X\times \{-1,0,1\})$. Pøijímá slovo $w$, pokud existuje nìjaký výpoèet $q_{0}w\vdash^{\ast} upv$ tak, že $p\in F$.
\end{definiceN}

\begin{poznamka}
Nedeterministické Turingovy stroje pøijímají právì rekurzivnì spoèetné jazyky, tj. nejsou silnìjší než deterministické. Výpoèty nedet. stroje lze totiž díky nekoneènosti pásky simulovat deterministickým (napø. prohledáváním do šíøky).
\end{poznamka}

\begin{definiceN}{Lineárnì omezený automat}
\emph{Lineárnì omezený automat} je nedeterministický Turingùv stroj s omezenou páskou (napø. symboly $l$ a $r$, které nelze pøepsat ani se dostat mimo jejich rozmezí). Slovo je pøijímáno, pokud $q_{0}lwr \vdash^{\ast} upv$, kde $p\in F$. Prostor výpoètu je omezen délkou vstupního slova. Lineárnì omezené automaty pøijímají právì kontextové jazyky.
\end{definiceN}


\begin{poznamkaN}{Rozhodnutelnost}
Turingùv stroj mùže nepøijmout slovo buï skonèením výpoètu v nekoncovém stavu, nebo pokud výpoèet nikdy neskonèí. Turingùv stroj \emph{rozhoduje jazyk} $L$, když pøijímá právì slova tohoto jazyka a pro libovolné slovo je jeho výpoèet koneèný. Takové jazyky se nazývají \emph{rekurzivní}.

Problém zastavení výpoètu Turingova stroje je algoritmicky nerozhodnutelný (kvùli možnosti jeho simulace jiným Turingovým strojem). Pro bezkontextové jazyky je algoritmicky rozhodnutelné, zda dané slovo patøí do jazyka. Pro bezkontextovou gramatiku nelze algoritmicky rozhodnout, zda $L(G)=X^{\ast}$. Pro dvì kontextové gramatiky je nerozhodnutelné, zda jejich jazyky mají neprázdný prùnik.
\end{poznamkaN}

\begin{reportN}{Hnetynka}
napisal som hierachiu, pravidla gramatik, ake automaty rozpoznavaju jednotlive gramatiky, pre reg gram veticky o KA a reg jazykoch. Pytal sa ma ako jednotlive automaty pracuju, zadal par jednoduchych prikladov a chcel zdovodnenie do akych tried patri -nakreslit/opisat slovami KA, gramatiky, ZA + dokaz pomocou pumping lemma. Pytal sa, do akej triedy by som zaradil rozpoznavanie prg jazykov, napr Java. Povedal som kontextove, ale zdovodnit som to velmi nevedel. Potvrdil ze su to kontextove + ze prave kvoli dobrym znalostam sa pouzivaju bezkontextove v kombinacii s analyzou kontextu (kedze na poradi jednotlivych riadkov zalezi) (znamka 1-2, ze zalezi na druhej inf otazke)
\end{reportN}

\begin{reportN}{Bulej}
napisal som gramatiky, odpovedajuce automaty, vysvetlil inkluzie a to bohato stacilo
\end{reportN}
\begin{reportN}{Bednarek}
Mìl jsem definice hierarchie a srovnání s pøíslušnými automaty, nástin (opravdu lehce) inkluzí mezi tøídami jazykù, Greibachové a Chomského n.f. plus nìjaké pøíklady jazykù, které dokazují ostrou inkluzi, ale bez pøesnìjších dùkazù (kouzelná vìta: "to se ukáže pøes pumping lemma" ;-) ) Ptal se mì na srovnání deterministických a nedeterministických verzí jednotlivých druhù automatù, což jsem vìdìl.
Informatika za jedna.
\end{reportN}
\begin{reportN}{Kucera}
Toto se obeslo bez problemu, stacilo to definovat, a rict ktere automaty prijimaij ktere jazyky, umet je definovat. Vety kolem moc zajem nevzbudily ( Nerudovka, pump. lemma):-) 
\end{reportN}
\begin{reportN}{Fiala}
Napsal sem tøídy automatù, gramatik, determinismus/nedeterminismus a pak ještì rozhodnutelnost. Pøi procházení sem dostával doplòující otázky typu : "Jak nìjak líp omezit odhad o nárustu poètu stavu pøi pøevodu NKA do KA" (záleží na poètu poèáteèních stavù a na poètu "stejnejch" pøechodù z jednotlivých stavù.), U rozhodnutelnosti náznak dùkazu halting problem a další.
\end{reportN}
\begin{reportN}{Bednárek}
Tak jsem napsal automat a gramatiku ke každé tøídì jazykù, determ./nedeterm. verze a jak je to kde s jejich silou. Drobné chyby v definicích nevadily, když byly po upozornìní opraveny. U RJ se zeptal ještì na reg. výrazy a pak taky proè že se rekurzivnì spoèetné jazyky jmenují jak se jmenují (kde je ta rekurze), což jsem nevìdìl a byl pouèen.
\end{reportN}

