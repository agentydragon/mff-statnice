\subsection{Algebraické algoritmy}

\subsubsection*{Diskrétní Fourierova Transformace (DFT)}

Diskrétní Fourierova transformace se pouívá, chceme-li zachytit hodnotu (pøepokládejme, e $2\pi$-periodické) funkce na intervalu $[-\pi,\pi]$ v nìjakıch $n$ bodech. To je dobré napø. pro vzorkování elektrického nebo zvukového signálu a jiné operace. Pro nìjakou funkci nám tak staèí znát vektor dimenze $n$ (a $n$ je poèet vzorkù na $2\pi$).

Je to zaloeno na Fourierovıch øadách -- dá se ukázat, e funkce $1$, $\cos kx$ a $\sin kx$ pro $k\geq 1$ tvoøí ortogonální bázi prostoru spojitıch funkcí na intervalu $[-\pi,\pi]$. Protoe potøebujeme znát jenom koneènı poèet vzorkù, staèí nám jen koneènı podprostor s koneènou bází. Máme-li rozklad nìjaké $2\pi$-periodické funkce do Fourierovy øady $f(x)= c + \sum_{k=1}^\infty a_k \sin k x + \sum_{k=1}^\infty b_k \cos k x$, dá se jednoduše ukázat, e pro hodnoty v bodech $-\pi,-\pi + \frac{\pi}{n}, -\pi + 2\frac{\pi}{n}, \dots, -\pi + (n-1)\frac{\pi}{n}$ staèí sumy do $\frac{n}{2}-1$ pro sinusové øady a $\frac{n}{2}$ pro kosinové -- vyšší koeficienty v takovıch bodech jsou nulové. Take $n$ hodnot funkce $f$ na intervalu $[-\pi,\pi]$ lze reprezentovat vektorem $n$ èísel v bázi $1,\cos x,\dots,\cos \frac{n}{2}x,\sin x,\dots,\sin(\frac{n}{2}-1)x$. 

Jednodušeji to lze ukázat v komplexních èíslech -- je známo, e 
$$e^{ix} = \cos x+ i\cdot\sin x$$
take vektor hodnot funkce lze ekvivalentnì reprezentovat v bázi $e^{i\cdot 2\pi\frac{k}{n}},\ k\in\{0,\dots,n\}$, nebo všechny vektory pùvodní báze lze zapsat jako lineární kombinace vektorù nové báze. Definujeme hodnotu
$$\omega := e^{i\cdot 2\pi\frac{1}{n}} \textrm{ (a to je vlastnì \uv{nìco jako} }\sqrt[n]{1}\textrm{)}$$
vidíme, e $\omega^k$ je $n$-periodická funkce, take nezáleí na hranicích sumace ($-\frac{n}{2}+1,\dots,\frac{n}{2}$ je ekvivalentní $0,\dots,n-1$). 
Potom se posloupnost $n$ komplexních èísel $\alpha_0, \dots, \alpha_{n-1}$ (napø. hodnot naší funkce v bodech $-\pi + \frac{2\pi k}{n},\ k\in\{0,\dots,n-1\}$) transformuje na posloupnost $n$ komplexních èísel $A_0, \dots, A_{n-1}$ (do báze $\omega^i,\ i\in\{0,\dots,n-1\}$) pouitím vzoreèku:
$$A_j = \sum_{k=0}^{n-1} \alpha_k \omega^{kj}  \;\;\;\;\; j = 0, \dots, n-1$$
Tento pøevod oznaèujeme jako \emph{diskrétní Fourierovu transformaci}.

\emph{Inverzní diskrétní Fourierova transformace} je opaènı problém -- z $n$ Fourierovıch koeficientù $A_k$ chceme zpìtnì vypoèítat hodnoty funkce $\alpha_k$ v bodech $-\pi + \frac{2\pi k}{n},\ k\in\{0,\dots,n-1$. Platí:

$$\alpha_j = \frac{1}{n}\sum_{k=0}^{n-1} A_k \omega^{-kj}  \;\;\;\;\; j = 0, \dots, n-1$$

\medskip
\begin{e}{Dùkaz}{0}{0}
Definujeme matici $W: W_{p,q}=\omega^{pq}$, potom $A = W\alpha$ (vektorovì), take $a = W^{-1}A$. Definujeme $W': W'_{p,q}=\omega^{-pq}$ a dokáeme, e $W\cdot W'= n\cdot I_n$. Máme
$$(W\cdot W')_{p,q} = \sum_{s=0}^{n-1} W_{p,s}\cdot W'_{s,q} = \sum_{s=0}^{n-1} \omega^{(p-q)\cdot s}$$
a potom pro
\begin{pitemize}
    \item $p = q$ platí $\sum_{s=0}^{n-1}\omega^{(p-q)\cdot s} = \sum_{s=0}^{n-1}\omega^0 = \sum_{s=0}^{n-1} 1 = n$
    \item $p\neq q$ definujeme
    $$Q:= \omega^{p-q}$$
    a dostaneme geometrickou posloupnost $Q^0 + Q^1 +\dots +Q^{n-1}$, pro její souèet prvních $n$ èlenù platí vzorec
    $$ \sum_{s=0}^{n-1} Q^s = Q^0 \frac{Q^{n-1+1} - 1}{Q-1} = 1\frac{1-1}{Q-1}= 0 $$
\end{pitemize}
\end{e}


\begin{e}{Algoritmus}{0}{Fast Fourier transform (FFT)}
Fast Fourier transform je algoritmus pro poèítání diskrétní Fourierovy transformace vektorù rozmìru $n=2^k$ v èase $\Theta(n\log n)$. Mám-li matici Fourierovıch koeficientù $W, W_{p,q} = \alpha_q \omega^{pq}$, mohu ji rozdìlit na liché a sudé sloupce, u sudıch vyjádøit $\omega^q$ a pro spodní polovinu øádek (se sumami jdoucími po dvou) mohu sníit exponent u $\omega$ o $n/2$ (díky periodicitì) a vyjdou stejná èísla:
\begin{align*}
    A_j &= \sum_{k=0}^{n-1} \alpha_k \omega^{kj}\ &j\in\{0,\dots,n-1\}\\
    \\
    A_j &= \sum_{k=0}^{\frac{n}{2}-1} \alpha_{2k}\omega^{2kj} + \omega^j \sum_{k=0}^{\frac{n}{2}-1} \alpha_{2k+1}\omega^{2kj}\ &j\in\{0,\dots,\frac{n}{2}-1\}\\    
    A_{j+\frac{n}{2}} &= \sum_{k=0}^{\frac{n}{2}-1} \alpha_{2k}\omega^{2k(j+\frac{n}{2})} + \omega^{(j+\frac{n}{2})} \sum_{k=0}^{\frac{n}{2}-1} \alpha_{2k+1}\omega^{2k(j+\frac{n}{2})}\ &j\in\{0,\dots,\frac{n}{2}-1\}\\
\end{align*}

\textit{Poznámka: pro rychlé a jednoduché pochopení tìch blektù co jsem tu napsal doporuèuji Kuèerùv program Algovision}\\ \texttt{http://kam.mff.cuni.cz/\~{}ludek/AlgovisionPage.html} \\ \textit{DFT je tam názornì a pøehlednì ukázaná.}
\end{e}

TODO: Související obecné \uv{vìci} o Fourierovì transofrmaci, pouití pøi spektrální analıze (Nyquist-Shannon sampling theorem), datové kompresi (Diskrétní kosinová transformace), násobení polynomù (+násobení velkıch integerù).

\subsubsection*{Euklidùv algoritmus}

Euklidùv algoritmus je postup (algoritmus), kterım lze urèit nejvìtšího spoleèného dìlitele dvou pøirozenıch èísel, tzn. nejvyšší èíslo takové, e beze zbytku dìlí obì èísla.

Algoritmus (pomocí rekurze):
\begin{verbatim}
function gcd(a, b)
    if b = 0 return a
    else return gcd(b, a mod b)
\end{verbatim}

Algoritmus (pomocí iterace):
\begin{verbatim}
function gcd(a, b)
    while b 
